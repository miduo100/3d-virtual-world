# virtual-world 部署说明

## 环境要求
- Node.js：推荐版本 20.x，最低版本 18.x
- 包管理器：NPM
- 数据库：PostgreSQL 18.1，最低版本 PostgreSQL 17
- 端口：3002

## 快速部署

### 1. 安装依赖

> **提示**：项目内已包含 `node_modules` 目录，可直接使用，无需重复安装。

如需重新安装依赖：
```bash
npm install
```

**Linux 用户**：项目根目录下的 `linux_node_modules.gz` 是 Linux 环境的预编译依赖包，解压后即可使用：
```bash
tar -xzf linux_node_modules.gz
```

### 2. 配置环境变量

> **请先完成数据库导入**：参照 `数据库导入.md` 文件中的步骤，先创建数据库并导入数据，再进行下面的 `.env` 配置。

项目根目录下已包含 `.env` 文件，直接用文本编辑器（如记事本、VS Code）打开编辑即可，无需在命令行中手动操作。

必须修改以下配置项：
- `DB_PASSWORD` — 数据库密码（填写你本地 PostgreSQL 的密码）
- `DB_NAME` — 数据库名称（默认 `virtual_world`）
- `DB_USER` — 数据库用户名（默认 `postgres`）
- `DB_HOST` — 数据库地址（本地为 `localhost`）
- `DB_PORT` — 数据库端口（默认 `5432`）
- `JWT_SECRET` — JWT 密钥（可用 `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` 生成）
- `ADMIN_JWT_SECRET` — 管理员 JWT 密钥（同上方式生成）
- `WORLD_NAME` — 世界名称
- `WORLD_URL` — 世界访问地址

### 3. 数据库导入

数据库已通过 `db_export.sql` 完整导出，直接导入即可使用。具体步骤请参照 `数据库导入.md`。

### 4. 启动服务
```bash
# 开发模式
npm start

# 生产模式（推荐 PM2）
npm install -g pm2
pm2 start src/server.js --name virtual-world
pm2 save
pm2 startup
```

### 5. 访问

**直接访问**：
- 用户端: http://你的服务器IP:3002/
- 管理后台: http://你的服务器IP:3002/admin.html
- 默认管理员: admin / admin123456（请立即修改）

**通过 Nginx 反向代理绑定域名访问**：
- 将域名解析到服务器 IP
- 配置 Nginx 反向代理，将 80/443 端口请求转发到本地 3002 端口
- 访问 `https://你的域名` 即可，无需带端口号

## 联邦系统配置

### 中央世界
```env
IS_CENTRAL_WORLD=true
AUTO_CONNECT_CENTRAL=false
```

### 子世界
```env
IS_CENTRAL_WORLD=false
CENTRAL_WORLD_URL=https://中央世界地址
AUTO_CONNECT_CENTRAL=true
```

## Docker 部署
```bash
docker compose up -d --build
```

## 目录结构
```
├── src/              # 后端源码
│   ├── server.js     # 主入口
│   ├── routes/       # API路由
│   ├── services/     # 业务服务
│   ├── middleware/   # 中间件
│   ├── database/     # 数据库模块
│   ├── websocket/    # WebSocket
│   └── utils/        # 工具
├── public/           # 前端资源
│   ├── js/           # 前端JS
│   ├── models/       # 3D模型
│   ├── uploads/      # 上传文件
│   └── *.html        # 页面
├── database/         # 数据库SQL（init.sql + migrations）
├── uploads/          # 根级上传目录（角色模板/建筑图片）
├── .env.example      # 环境变量模板
├── package.json      # 依赖配置
└── Dockerfile        # Docker配置
```

## 技术支持
济宁米多信息科技有限公司
联系方式: 888@miduo100.com / 15660440944
