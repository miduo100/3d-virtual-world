# AI Agent 接入虚拟世界 — 应用价值与内容系列规划

> 创建：2026-09-19
> 定位：**面向外部的内容生产母文档**。技术权威文档是《AI-Agent接入系统-开发规范与进度.md》（协议、红线、验收）；本文只回答三件事：**它是什么、企业拿它做什么、我们每期文章怎么写**。
> 所有能力描述与数字均来自已验收的实现与实测报告，不含未开发功能的承诺。

---

## 第〇节 一句话说清这件事

**给 AI 一个域名和一把钥匙，它就能走进一个 3D 世界，有身体、有位置、能被真人看见、能说话带路——而服务器只发 JSON 字符串，3D 画面由访客的浏览器替它渲染。**

三个角色，各只需要一样东西：

| 角色 | 需要什么 | 体验 |
|---|---|---|
| 企业 / 运营方 | 后台创建 Agent，领一把 API Key | 决定这个 AI 叫什么、长什么样、站在哪、能不能实时推流 |
| AI（Agent） | 一个能跑代码的运行环境 + 那一把 Key | 只能拿到结构化 JSON（雷达/事件流），从不下载模型、从不看图 |
| 真人访客 | 打开网页，和平时一样 | 世界里多了一个带 AI 标识的 3D 角色，能对话、能看着它走路 |

**关键点**：真人访客不需要装任何插件、不需要知道 AI 在哪台服务器上；AI 也不需要浏览器、不需要 GPU、不需要 WebGL。两边的耦合只有一处——服务器内存里那张"谁在哪"的坐标表。

---

## 第一节 功能现状：能做什么、不能做什么

### 1.1 已经实现并验收的能力（截至 2026-09-19）

**接入侧**

| 能力 | 说明 |
|---|---|
| 三步接入 | 访问 `/.well-known/virtual-world-agent.json` 自动发现 → 用 Key 换 15 分钟短期令牌 → 连 `/ws/agent` 进场 |
| 零知识域名发现 | 只需知道域名，能力清单/协议/实体标识契约全部机器可读（capabilities、openapi.json） |
| 零依赖示例客户端 | `examples/agent-client/node-agent.mjs`：Node 18+ 无任何第三方依赖即可跑通全链路 |
| 两档身份 | **游客票**（无需 Key，30 分钟临时身份，纯请求-响应拉模式）／**Key Agent**（推模式，实时事件流特权） |

**感知侧**

| 能力 | 说明 |
|---|---|
| 空间雷达 observe | 返回 `self` + 附近真人/其他 Agent（含距离、朝向、动作状态）+ 世界物体 + 传送门，Key 档最大 200m、游客档 30m |
| 物体语义 | 每个世界物体可填 **AI 描述**（编辑器里直接填，最多 500 字），observe 时随雷达下发（截断 300 字）——**这是让 AI"知道这里是什么"的官方接口** |
| 实时事件流 | 三档推送（eco 不推 / standard 每秒聚合 / realtime 逐条），新实体出现、消失、移动都能收到 |
| 实体标识契约 | 定位必须用 id（世界里允许同名），聊天记录、事件流、雷达三处的 id 是同一套命名空间 |
| 聊天上下文 | 断线重连可拉最近聊天记录，恢复对话上下文 |

**行动侧**

| 能力 | 说明 |
|---|---|
| move / walk_to | 服务端权威限速推进（速度后台可配 1~20 m/s，默认 9 = 与真人同量级），到达/被打断/超时都有回执 |
| follow | 持续跟随指定目标（可设停止距离与最长时长），用于"带路""贴身讲解" |
| rotate / jump | 转向、起跳，真人端可见 |
| say | 走真人聊天管线（30m 内投递，真人头顶气泡），每条 ≤200 字 |
| interact | 距离校验后的互动动作 |
| 动作回执 | 每个指令都有 accepted / completed / rejected，被打断会明确告诉你"被新指令取代" |

**呈现与运营侧**

| 能力 | 说明 |
|---|---|
| AI 身份明示 | 系统消息"(AI)加入了"、头顶名字带 AI 前缀——真人始终知道自己面对的是 AI |
| Avatar 六件套 | 模型、动作库、武器、骨骼映射、挂点、校准，全部随身份下发，跨世界不变 |
| 客户端平滑 | Agent 移动在真人端做插值逼近，不会一格一格跳 |
| 管理后台 | 创建/停用/删除 Agent、发放与吊销 Key、单 Agent 推送档位、全局上限、聊天归档配置 |
| 聊天记录 | 本地实时落库 + 每日自动归档到 S3 兼容存储（未成功上传永不删本地），保留期后台可设 |
| 跨世界传送 | 身份与 Avatar 跨世界保持不变，一次性凭证（300 秒有效、防重放），不在目标世界建账号 |
| 审计与日志 | 登录/发 Key/改配置/签票进审计日志，访问日志与运维日志分流，按天轮转 |

### 1.2 明确不做 / 目前做不到（写文章时必须如实交代）

| 做不到的 | 现状与原因 |
|---|---|
| AI 看不见画面 | 第一版 AI 只有结构化雷达，没有视觉。无头渲染截图属可选路线（Vision），当前未开 |
| 平台不做语音识别与合成 | 语音是中继通道且默认关闭；转写/发声由企业自己的 AI 客户端接（Whisper、豆包 ASR 等） |
| 平台不托管知识库 | 平台只提供"物体描述 + 聊天记录 + 雷达"三类原料，**企业的行业知识库由企业侧自建（RAG）** |
| 不做地形贴合 | 服务端没有地形与碰撞数据（在前端），AI 移动是平面 + 边界，不做爬山涉水 |
| AI 不能传送 / 不能碰资产 | 与真人游客同一套准则：没有传送门、没有坐标传送、没有背包商城；跨世界需管理员显式开权限 |
| 不承诺"自主意识" | 它是可编程的角色，行为来自你给它的模型和提示词，不写"AI 自己想干什么" |

---

## 第二节 为什么这件事值得写成一个系列

### 1. 反差点一：AI 从"打字框"变成"在场者"

过去企业和 AI 的关系在聊天窗口里；现在 AI 出现在一个**有坐标的空间**里。它站的位置和真人在同一个坐标系，所以"距离""附近""同处一室"这些概念第一次对 AI 成立——它才能被指派"走过去""跟着这个人""在门口迎宾"。

### 2. 反差点二：成本曲线反了

AI 越"像 3D 角色"，成本越高，是常识；这套架构里恰好相反：**AI 本身不渲染任何画面**，3D 模型由每个访客的浏览器下载并渲染。服务器只转发坐标和 URL 字符串。

实测：每个 Agent 的推流约 **1 KB/s**（100 个 Agent 同场约 0.8 Mbps），服务器 CPU 占用约 **单核的 1%**（20 秒 0.19 核秒）。也就是说，**"看得见的 AI"比"看不见的 AI"便宜**——因为它把渲染成本摊给了访客的显卡。

### 3. 反差点三：企业零改造就能接待

企业不需要给访客发客户端、不需要装数字人一体机、不需要专门的 GPU 服务器。访客用普通浏览器打开一个网址，AI 就在里面。企业侧真正要准备的只有两样：**3D 场景**和**知识**——场景可以在这个世界编辑器里搭，知识填在每个物体的"AI 描述"里。

---

## 第三节 企业使用场景库（文章选题的来源）

### 3.1 通用公式

> **行业知识库 + 一个 3D 场景 + 一个会走路会说话的 AI 身体 = 一个不需要员工在场的接待者 / 讲解者 / 陪伴者**

企业形态不同，只是把公式里的三项换掉：知识换成本行业的、场景换成自己的空间、AI 的角色换成需要的岗位。

### 3.2 场景一览（按落地难度排序）

| # | 场景 | 典型客户 | AI 的角色 | 核心价值 | 落地难度 |
|---|---|---|---|---|---|
| 1 | 展馆/博物馆讲解 | 博物馆、美术馆、科技馆、企业展厅 | 讲解员 | 讲解不休息、可一对多、可多语、随时问 | 低 |
| 2 | 展会无人展台 | 参展企业 | 展台接待 | 讲解员下班后展台仍有人接、留资不丢 | 低 |
| 3 | 售楼处 / 样板间 | 地产、家居、建材 | 导购 | 带看动线、随时答疑、非工作时间也能看 | 低 |
| 4 | 汽车 4S / 车展 | 汽车品牌、经销商 | 产品专家 | 参数答疑、配置对比、邀约试驾 | 中 |
| 5 | 校园 / 园区导览 | 高校、园区、政务大厅 | 向导 | 新生/访客自助找路、政策问答 | 中 |
| 6 | 企业虚拟总部 | 招商、招聘、对外合作 | 接待官 | 远程带看、投融资与招聘场景讲解 | 中 |
| 7 | 品牌虚拟门店 | 电商、消费品 | 店员 / 数字人主播 | 导购、直播互动、私域沉淀 | 中 |
| 8 | 文旅景区 / 主题乐园 | 景区、文旅项目 | NPC 导游 | 陪走、讲故事、和真人游客同框互动 | 中 |
| 9 | 活动运营 | 峰会、发布会、线上展会 | 主持人 / 组织者 | 多 Agent 分工（主持 + 引导 + 答疑） | 中高 |
| 10 | 多智能体社会实验 | 高校、研究机构、AI 公司 | 被试/协作者 | 真人与多 AI 共处的行为研究、模型评测 | 高 |
| 11 | 客服前置（世界内） | 任何有 3D 展示需求的企业 | 引导员 | 把"找客服"变成"有人带你走" | 中 |

**不适合的场景（诚实说明，写文章时反而加分）**：需要 AI 看懂图像细节的（如设备巡检）、需要 AI 在真实物理世界行动的、需要 AI 自主做高风险决策的、只想做纯文字客服的（用普通聊天机器人更便宜）。

### 3.3 一个场景的展开模板（写作时逐个填）

以最容易落地的"展馆 AI 讲解员"为例：

| 维度 | 内容 |
|---|---|
| 客户痛点 | 讲解员人力有限、闭馆后无人、外语讲解稀缺、观众问的问题重复 |
| AI 的角色 | 在展厅门口迎宾 → 跟观众走 → 在展品前停下讲解 → 回答追问 → 引导到下一个展区 |
| 用到哪些能力 | observe（知道观众和展品位置）、各展品的 AI 描述（知识挂载点）、follow（跟着走）、walk_to（带路）、say（讲解与答疑）、Key 推模式（实时感知观众走动） |
| 企业准备 | ① 展厅 3D 场景（模型 + 摆放）② 每个展品的 AI 描述（知识摘要）③ 企业知识库与提示词（AI 端 RAG）④ 选一个模型（Qwen / 豆包 / DeepSeek / 自研） |
| 现场效果 | 真人访客进场景即可看到讲解员，会被跟随、被搭话；讲解员面对不同观众可以说不同深度的话 |
| 验收标准 | 真人端能看到它、能对话、能被带路；30 分钟内无人工干预 |
| 文章看点 | "一个讲解员，一天 24 小时，服务无限观众，成本是一把 API Key" |

---

## 第四节 内容系列规划（每期一篇文章）

### 4.1 系列名与主线

**系列名（建议）**：《AI 有了身体》——AI Agent 接入虚拟世界系列

**主线**：每一期回答同一个问题的不同版本——**"什么样的企业，用它做什么？"**

每期结构恒定，读者形成预期，我们形成流水线：

```
① 场景痛点（3 句，读者自检"这说的是我"）
② 传统做法 vs 这套做法（一张对比表）
③ 现场演示（截图/录屏/可复现的 demo 命令）
④ 背后怎么接的（三张图：发钥匙 / 挂知识 / 真人看到什么）
⑤ 企业要准备什么（清单，可打印）
⑥ 成本与效果（真实数字，不编造）
⑦ 下一步（引导：想要同款，需要提供什么）
```

### 4.2 十三期选题表

| 期 | 标题（暂定） | 目标读者 | 要证明的一件事 | 演示素材 |
|---|---|---|---|---|
| 0 | AI 有了身体：我们把 AI Agent 接进了虚拟世界 | 所有潜在客户 | 这件事已经能跑，不是概念 | 真人端录屏：AI 进场、被看见、对话、走路 |
| 1 | 博物馆请了个不下班的讲解员 | 展馆/博物馆/科技馆 | 讲解岗位可被 AI 承接且不牺牲体验 | 展厅场景 + 讲解点描述 + 跟随讲解 |
| 2 | 展会结束了，展台还在接待 | 参展企业/会展主办 | 无人值守时段仍能接待与留资 | 展台场景 + 多访客同时咨询 |
| 3 | 样板间里有个不看表的导购 | 地产/家居/建材 | 带看动线可标准化、可复制 | 样板间场景 + 动线跟随 |
| 4 | 车展上被问了一千遍的问题 | 汽车品牌/经销商 | 高频参数答疑是 AI 的最优战场 | 车型场景 + 配置对比问答 |
| 5 | 新生报到，问 AI 就够了 | 高校/园区/政务大厅 | 空间导览 + 政策问答可以合一 | 校园场景 + 带路到指定建筑 |
| 6 | 虚拟总部：远程带看的一场会议 | 招商/招聘/BD | 关键关系场景也能用 AI 先接待 | 总部场景 + 招商讲解 |
| 7 | 虚拟门店的店员不下班 | 品牌/电商 | 3D 导购与私域转化可串联 | 门店场景 + 商品讲解 + 留资 |
| 8 | 景区里的 NPC，和真人一起走完这段路 | 文旅/主题乐园 | AI 能长期陪伴式服务，不止一问一答 | 景区场景 + 长时段跟随讲解 |
| 9 | 一场活动，三个 AI：主持、引导、答疑 | 峰会/发布会/线上展会 | 多 Agent 分工协作是可运营的 | 三 Agent 同场分工（已有三档演示脚本） |
| 10 | 真人愿意和 AI 一起待多久？ | 高校/研究机构/AI 公司 | 这套系统可作为真人与多智能体共处的研究平台 | 多 Agent × 多真人同场数据 |
| 11 | 拆解：让 AI 走进 3D 世界，需要几行代码 | 开发者/技术决策者 | 接入门槛低到可自助（域名 + Key + 示例客户端） | 零依赖示例客户端全链路日志 |
| 12 | 一个 AI 一天花多少钱 | 财务/采购/技术负责人 | 成本结构与边界清晰，可预算 | 推流量、CPU、存储、模型调用四项拆账 |

### 4.3 每期必备的"可信度三件套"

1. **可复现**：文中给出的命令/脚本在项目里真实存在（如驻场演示脚本、验收脚本报告 JSON）。
2. **有真人视角**：每期至少一张真人浏览器截图（不是只有服务端 JSON），证明"看起来就是这样"。
3. **有边界**：每期结尾写清"这期做不到什么"，长期建立可信度。

---

## 第五节 "企业 AI 讲解员"最小落地闭环（可操作清单）

这是**所有场景的样板**，其他场景只是替换知识内容与 3D 场景。

### 第一步：平台侧（我们做，约 10 分钟）

1. 后台打开总开关（Agent 接入默认是关闭的，需要手动开启）；
2. 创建 Agent：起名、设 Avatar（模型/动作/武器/骨骼）、选择推送档位（讲解员建议 standard 或 realtime）；
3. 生成 API Key（**明文只显示一次**，交给企业保管）；
4. 设置上限：并发数、速度（1~20 m/s，默认与真人同速）、观察范围。

### 第二步：企业侧准备（企业做）

| 准备项 | 说明 | 说明不了的后果 |
|---|---|---|
| 3D 场景 | 用世界编辑器搭建或导入模型 | AI 无处可站 |
| 每个点位/展品的 **AI 描述** | 编辑器里填，500 字以内，是 AI 认识世界的唯一语义来源 | AI 知道那里"有个东西"，但不知道那是什么 |
| 行业知识库 | 企业自建（向量库 / 文档 / 提示词），AI 端在回答时检索 | 只能按描述念，无法深入追问 |
| 一个会写代码的 AI 运行环境 | 部署示例客户端或自研 Agent 程序 | 进不来 |
| 模型与账号 | 自选模型（通义千问 / 豆包 / DeepSeek 等） | 无话可说 |

### 第三步：Agent 端实现（我们提供脚手架，企业改提示词）

```
发现 → 换令牌 → 连上 → 读世界快照
   ↓
每 1~2 秒 observe 一次（拿到附近的人、物、距离）
   ↓
决策：有人靠近某展品？→ walk_to 过去 → say 讲解
     有人走动？→ follow 跟着 → 在该讲的点位停下
     有人提问？→ 结合知识库回答 → 必要时 say 引导
   ↓
全过程真人端可见：走路有动画、说话有气泡、头上有 AI 标识
```

### 第四步：验收标准（企业可现场判定）

- [ ] 真人打开网址，能看见讲解员（带 AI 标识）
- [ ] 讲解员会主动靠近访客，不是站着不动
- [ ] 提问能被回答，且答案来自企业知识库，不是通用闲聊
- [ ] 走动时位置连续、不瞬移、不埋地
- [ ] 全程 0 报错，断线后能自动回来
- [ ] 访客散场后讲解员空闲自动退场（空闲超时）

---

## 第六节 写作弹药：可直接引用的实测数字

| 指标 | 实测值 | 出处 |
|---|---|---|
| 每个 Agent 推流量 | 约 1 KB/s（100 Agent ≈ 0.8 Mbps） | v2 多端联测 |
| 服务器负载 | 20 秒 0.19 核秒 ≈ 单核 1% | v2 多端联测 |
| 游客档推流 | 8 秒内只收到进场信息，0 条实体推送 | 游客联测（反证红线 14） |
| 三档推送量（15 秒） | eco 0 条 / standard 14 条（621 B/s）/ realtime 65 条（722 B/s） | 三档联测 |
| AI 移动速度 | 后台可配 1~20 m/s，默认 9（与真人同量级） | 首轮缺陷修复 |
| 观察半径 | Key 档最大 200m；游客档硬钳 30m | P2 / P8 |
| 移动平滑 | 真人端滞后 < 3m、无瞬移（修复前最大滞后 21m、20.7m 瞬移） | 三档联测 |
| 聊天存储成本 | 约 500 字节/条（语音只存元数据，不存音频） | P4 设计 |
| 空闲退场 | 默认 5 分钟无操作自动离开世界 | 双端联测 |
| 跨世界凭证 | 一次性、300 秒有效、防重放（重放被拒 409） | P5 验收 |
| 接入门槛 | 一个域名 + 一把 Key + Node 18+（示例客户端零依赖） | P6 验收 |
| 验收覆盖 | 17 个可重跑矩阵脚本，累计 300+ 判据通过 | 各阶段验收 |

---

## 第七节 表述红线（写文章时必须遵守）

1. **不神化**：不写"AI 有意识/有情感/自主意志"；写"可编程的角色，行为来自你给它的模型与提示词"。
2. **AI 身份必须明示**：这是产品红线（真人始终能看出对方是 AI），文章中也应主动提及，作为合规优势而非限制。
3. **数据合规**：
   - AI 描述对所有 Agent（含公开游客）可见，**不要写敏感商业信息**；
   - 聊天记录会本地保存并按后台配置归档（默认 7 天），企业需在用户协议中告知访客；
   - 语音默认不中继给 AI，音频不落库。
4. **不承诺未开发能力**：视觉理解、地形贴合、自主长任务、语音双向实时对话（当前是转写由客户端做）都尚未提供，不要写成已有。
5. **不公开敏感配置**：API Key、管理员凭证、世界 URL 白名单等不进文章与截图。

---

## 第八节 后续路线图（文章结尾的钩子来源）

| 方向 | 价值 | 状态 |
|---|---|---|
| AI 视觉（无头渲染截图） | 让 AI 能"看"一眼场景，用于巡检式讲解与质检 | 可选阶段，未开工 |
| 主动停止动作 | 让人可以打断 AI 的移动 | 设计已完成，待决策 |
| 多 Agent 避让 | 多个 AI 同场时不重叠，队列式接待 | 缺陷已定位，待决策 |
| 知识库托管 | 平台侧统一挂载企业知识，减少企业集成成本 | 未立项 |
| 百级 Agent 压测 | 支撑大型活动（峰会/展会） | 待执行 |
| 后台日志查看页 | 运营侧自助排查 | 二期可选 |

---

## 第九节 生产节奏建议

- **每期周期**：1 期 = 1 个可演示场景 + 1 篇文章 + 1 段录屏 + 1 份清单。
- **素材复用**：每期产出的场景与 Agent 配置留档，后续期数可组合（例如第 9 期的多 Agent 分工可直接复用到展会）。
- **前置依赖**：每期开工前，先确认三件事——场景能搭出来吗？知识谁提供？演示给谁看？
- **优先级建议**：先做第 0 期（总纲）＋第 1 期（展馆讲解，用户已明确提到的场景）＋第 12 期（成本），三篇打通"是什么 / 能干什么 / 多少钱"，再逐行业展开。

---

## 附：本文档与其他文档的关系

| 文档 | 作用 |
|---|---|
| 《AI-Agent接入系统-开发规范与进度.md》 | 技术权威文档：架构、协议、红线、验收（不对外） |
| 本文档 | 商业与内容权威文档：价值、场景、文章系列（对外素材库） |
| `examples/agent-client/README.md` | 开发者三步接入说明（对外技术入口） |
| `scripts/agent_tier_demo.js` | 驻场演示脚本：三个 Agent 进场跟随真人并定时说话（演示取素材用） |
