# 近期功能与技术优势说明

> 整理时间：2026-09-22
> 用途：对外介绍（简版，第 1~2 章）｜对内说明与选型依据（详版，第 3~5 章）
> 说明：本文所有指标均来自项目内可重跑的验收脚本（`scripts/accept_*.js`），第 5 章如实标注了当前代码中尚未恢复的部分。

---

## 目录

- [第 1 章 一句话总结](#第-1-章-一句话总结)
- [第 2 章 对外简版：我们做了什么、有什么好处](#第-2-章-对外简版我们做了什么有什么好处)
  - [2.1 十件事一览](#21-十件事一览)
  - [2.2 可以量化的感受](#22-可以量化的感受)
  - [2.3 如果只能说三句](#23-如果只能说三句)
  - [2.4 可以直接发给别人的一段话](#24-可以直接发给别人的一段话)
- [第 3 章 对内详版：逐项技术优势](#第-3-章-对内详版逐项技术优势)
  - [3.1 总览](#31-总览)
  - [3.2 渲染内核：Three.js r128 → r185（WebGL2）](#32-渲染内核threejs-r128--r185webgl2)
  - [3.3 3D 高斯泼溅（3DGS）](#33-3d-高斯泼溅3dgs)
  - [3.4 模型 LOD 三版本体系](#34-模型-lod-三版本体系)
  - [3.5 AI Agent 接入系统](#35-ai-agent-接入系统)
  - [3.6 卡顿治理](#36-卡顿治理)
  - [3.7 GLB 资产手术工具链](#37-glb-资产手术工具链)
  - [3.8 骨骼与动画兼容套件](#38-骨骼与动画兼容套件)
  - [3.9 实时通信与多人体验健壮性](#39-实时通信与多人体验健壮性)
  - [3.10 跨世界联邦传送](#310-跨世界联邦传送)
  - [3.11 世界的 AI 可读性与资产治理](#311-世界的-ai-可读性与资产治理)
- [第 4 章 贯穿性工程能力](#第-4-章-贯穿性工程能力)
- [第 5 章 现状说明与后续机会](#第-5-章-现状说明与后续机会)

---

## 第 1 章 一句话总结

**技术版**：最近这批工作把世界从"能逛但卡、资产粗糙、只能人玩"，升级为"**渲染内核现代化（r185/WebGL2）+ 实景化（3DGS）+ 成本工程化（LOD/资产手术/卡顿治理）+ 对外开放（AI Agent 一等公民）**"四位一体的形态。

**大白话版**：把这个世界从"能逛，但卡、粗糙、只能人玩"，升级成了**跑得动、看着真、别人也能接进来玩**的世界——尤其是**能让外面的 AI 进来，像真人一样在里面走路、说话，还能被真人看见**。

---

## 第 2 章 对外简版：我们做了什么、有什么好处

### 2.1 十件事一览

| # | 我们做了什么 | 说人话的好处 |
|---|---|---|
| 1 | **3D 引擎整体换代**（老引擎换新引擎） | 画面颜色变准了、不再是"粉粉的"假色；不再依赖外网，内网/离线也能跑，不会因为外网抽风打不开 |
| 2 | **支持"实景扫描"进世界**（3D 高斯泼溅） | 可以把**真实拍出来的场景**搬进世界，看着像照片/实景，不是塑料模型感；主流工具扫出来的文件都能直接用 |
| 3 | **模型自动做"高清/标清/简版"三档**（LOD） | 走近看精细、走远自动换成简版。好处：**不再卡**，而且远处**还能看见东西**（以前远处直接变蓝方块） |
| 4 | **开放 AI 接入** | 外面的 AI（GPT、Claude、自家 AI 都行）可以**像真人一样进世界**：有身体、会走路、会说话、能被真人玩家看到。**别人只要一个网址就能接进来**，不用装我们的软件 |
| 5 | **专门治"卡顿"** | 进世界不再长时间定住；转动视角不顿；加载有进度条、模型一个个出现，而不是画面死住 |
| 6 | **模型自动"瘦身"** | 一个模型文件体积能砍掉一半到九成，**下载快、省硬盘、省流量**，手机上也不容易崩 |
| 7 | **别人家的模型和动作直接能用** | 不同软件导出的角色模型、动画，以前要人工一个个修，现在**基本自动适配**，拿来就能动 |
| 8 | **掉线自动恢复 + 近距离语音** | 网络一抖、笔记本合盖再打开，**不用刷新页面**就自动恢复（以前要双方各自刷新才互相看得见）；30 米内可以按住说话聊天，人多了也能按服务器档次设上限 |
| 9 | **跨世界传送更顺了** | 从我们的世界传到另一个世界，**账号身份跟着走**，不会再出现"过去了变成游客"的情况 |
| 10 | **给世界里的东西写"AI 能看懂的说明"** | AI 进来能知道那是"课桌""老师""自行车"，而不是只知道一串坐标——AI 才真的会用这个世界 |

### 2.2 可以量化的感受

- 远处模型的负担**降了七到八成**，同一个场景里原来跑不动的，现在能跑顺。
- 模型文件**瘦掉 68%~92%**，打开速度明显变快。
- **100 个 AI 同时在线**，服务器 CPU 只用到**不到一成**，内存不到 100MB —— 也就是"能接很多 AI，成本却很低"。
- 真人玩家端：画面**稳定 60 帧**，零报错。

### 2.3 如果只能说三句

1. **画面和性能一起升级了**：看着更真，跑得更顺，远处也不会变成一片方块。
2. **AI 能进我们的世界当居民**：会走会说、被真人看见，而且**外面的人拿一个网址就能接**——这是我们和别人最大的差别。
3. **门槛极低、成本极低**：别人不用装软件、不用买我们的东西，我们这边接 100 个 AI 服务器几乎没压力。

### 2.4 可以直接发给别人的一段话

> 我们最近把虚拟世界做了一次比较大的升级。一是**画面和流畅度**：3D 引擎换代、颜色更真实，所有模型自动做高清/标清/简版三档，走近看细节、走远自动简化，原来的卡顿基本消失，模型文件也小了一大半，加载更快。二是**实景支持**：真实拍摄扫描出来的场景可以直接放进世界，看起来像实景。三是**我们开放了 AI 接入**——外面的 AI 只要一个网址，就能以"一个角色"的身份进到世界里，会走路、会说话，真人玩家能实时看见它，服务器接 100 个 AI 也只占用不到一成 CPU。另外断线能自动恢复、近距离能语音聊天、跨世界传送身份也能跟着走。简单说：**这个世界现在既能真实地看，也能让 AI 真的进来生活。**

---

## 第 3 章 对内详版：逐项技术优势

### 3.1 总览

| 功能簇 | 技术定位 | 关键可复现指标 |
|---|---|---|
| Three.js r185 升级 | 渲染内核 r128 → r185 / WebGL2 | 6 阶段全验收，REVISION=185，颜色管线正确化 |
| 3D 高斯泼溅（3DGS） | 照片级实景重建进世界 | 5 种格式、自研解析+渲染、24000 点真实绘制、26/26 |
| 模型 LOD 三版本体系 | 上传即自动生成三级模型 | 118 组变体、低模全库 ≤100 面、三点位三角数 −71%~−87% |
| AI Agent 接入 | 让外部 AI 成为世界一等公民 | 8 动作 / 3 档推送 / 100 Agent 实测 0.079 核 / 13 条红线 |
| 卡顿治理 | 把"卡"变成可归因、可预算 | draw call −73%、灯数变化零重编译、解析入 Worker |
| GLB 资产手术工具链 | 不改几何、无损重写 GLB 二进制 | 贴图 −68%、变体贴图剥离 −1.3GB、面数 ≤100 |
| 骨骼/动画兼容套件 | 跨平台模型的"翻译层" | 轨道命中 33/33、312/318；骨骼回归 67/67 |
| 实时通信健壮性 | 弱网与后台标签页自愈 | 重连后对端仍可见我移动 9/9；语音名额制 |
| 跨世界联邦传送 | 跨世界身份与资产通道 | 同名冲突 500→自动改名 4/4；协议表头修正 15/15 |
| 世界的 AI 可读性 | 物体语义描述 + 资产治理 | Agent 可读 description；孤儿资产 3.1GB→390MB |

### 3.2 渲染内核：Three.js r128 → r185（WebGL2）

**做法**：esbuild 自建 r185 UMD bundle 顶替旧文件名 → 6 个加载器本地化/stub 化 → `three-shim` 垫片（87 符号）→ postprocessing 6.39.4 本地 vendor → importmap 从 CDN 改指本地（消除双实例红线）→ 兼容层 `patchTHREE` 把旧 `outputEncoding`/`texture.encoding` 真映射到新 `colorSpace` API。

**技术优势**

- **颜色正确性**：旧 r128 基线其实是错的（sRGB 输出 + 贴图按线性着色 = 双重 gamma，画面偏亮偏粉）；r185 是 r152 颜色管理革命后的正确管线。这是"看得见的画质升级"，不是版本数字游戏。
- **WebGL2 能力面**：3DGS 自研 shader、BVH 碰撞、InstancedMesh 合批全部吃到 WebGL2 红利。
- **去 CDN 化**：可离线/内网部署，不受 CDN 抖动与版本漂移影响；编辑器群 + 管理后台 + 游戏页全部本地依赖。
- **79 处遗留 API 清单化治理 + 兼容层兜底**：存量用户代码块不用逐个改写。
- **降级友好**：`webgl2Guard.js` 在加载 three 之前检测，不支持时全屏中文提示 + `window.stop()`，不白屏、不报错刷屏。

**指标**：10 张统一口径基线截图 + 自研 PSNR/diff 工具（admin 43.4dB 一致；主世界差异全部归因颜色正确化，构图/建筑/角色像素级一致）。

### 3.3 3D 高斯泼溅（3DGS）

**做法**：全自研链路 —— `gaussianSplatLoader.js`（二进制 PLY 解析，含 SH 系数 `f_rest_0..8`）+ `gaussianSplatRenderer.js`（BufferGeometry + ShaderMaterial + Points，一次上传全量 GPU）+ `world3dgs.js`（接入世界对象生命周期：占位、卸载、dispose）+ `routes/threeDgs.js`（场景库只读列表、同名缩略图自动识别、DB 注册名优先排序）。

**技术优势**

- **照片级真实感**：与几何建筑、GLB 模型在同一世界里共存混合。
- **训练端不必迁就我们**：格式面宽（`.rad/.ply/.spz/.splat/.ksplat`），主流 3DGS 工具导出都能接。
- **零第三方 3DGS 库依赖**：自研解析与渲染，GLSL1 写法在 r185 WebGL2 下自动编译通过，且手动 `pow(1/2.2)` 不构成双重编码（已实测确认）。
- **资产库化**：`public/scenes/3dgs` 目录即素材库，世界编辑器"高斯"按钮直接挑选，注册名/未注册分组排序。
- **生命周期与 GLB 同位**：卸载走 `disposeSplat` + 摆回占位方块，长会话不泄漏。

**指标**：阶段 5 验收 26/26；真实渲染 `renderer.info.render.points=24000`；样本为脚本生成的 INRIA 标准 62 属性 PLY。

### 3.4 模型 LOD 三版本体系

**做法**：上传即生成（`gltfpack -sa` + 自研 `glbFaceReducer` 贪心边坍缩兜底）→ 按**玩家到模型表面距离**分带（不是锚点距离）→ 两条渲染路径（合批组 `worldInstanceMerger_v2` + 散装 `worldLodStandalone` 旁路，`world.js` 零改动）→ 变体懒加载 + LRU + **借高模材质共享 Texture** → 后台三输入框热调分带、60s 内自动跟进。

**技术优势**

- **通用能力而非特例**：所有上传模型自动获得三级变体，不需要为某个热点单独优化。
- **绝对面数保证**：低模全库 **118/118 ≤100 面**——超出 gltfpack 拓扑下限的部分由自研坍缩器兜底，这是"远景成本近零"的关键。
- **变体零额外显存**：变体借高模材质（同一 Texture 对象，断言 33/33、36/36）。
- **磁盘治理**：变体贴图自动剥离，磁盘 −1.3GB；离线部署包不含变体（现场 118 条目 36.7s 生成）。
- **口径正确**：用"表面距"而非"锚点距"，大半径模型（脏盒曾达 155m 宽）不会被误判为远/近。
- **可运维**：后台一键批量生成、状态计数准确（pending 仅剩 1 个 Draco 工具链限制）、`_lod.skip.json` 防"永远显示待生成"。
- **会话态自愈**：脏包围盒 30s TTL + 锚点一致性抽检；变体请求失败 60s 冷却重试（不再是永久回退高模）。

**指标**：三机位开/关三角数 —— 教室热点 **−85.8%**、出生点 **−71.2%**、红军质心 **−86.7%**；中景公平对比点 13.81M→5.08M（**−63.2%**）；生命周期 3 轮往返无泄漏（可达几何 uuid 集完全一致）；红军族低模总量 −98.5%。

### 3.5 AI Agent 接入系统

**做法**：HTTP `/api/agent/v1` + 专用 WS `/ws/agent`（`upgradeRouter` 按路径分流，**其余一切路径兜底给人类**，零破坏现网）；唯一焊接点 `agentPresenceBridge` 写 `playerPositions` → 复用现有 `PLAYER_JOINED / POSITION_UPDATE / CHAT` 广播，真人浏览器零大改即可看到 AI 的 3D Avatar；身份三档（游客公开票 / Key 推流特权 / 跨世界 transient）；8 个动作（move/walk_to/follow/rotate/jump/say/interact/stop）；三档推送（eco 纯拉 / standard 1s 聚合 / realtime 10Hz）；三个公开发现端点；零依赖 Node 示例客户端。

**技术优势**

- **AI 不需要浏览器、截图或视觉**：纯结构化 JSON 雷达；GLB 从不下给 AI，AI 的 Avatar 由**真人的浏览器**替它渲染——这是成本与复杂度的双重胜利。
- **"门禁倒过来"的开放生态**：无 API Key 也能凭域名拿到 30 分钟公开票进场（拉模式），只有稀缺的推流资源才需要 Key。这个决策直接对应"上线初期风险是没人来，而不是被滥用"。
- **安全与成本是内置的**：13 条红线（游客永不推流、观察半径硬钳 30m、位置流不消耗令牌桶、背压 1MB 警告/4MB 断开、30s 心跳、空闲踢出、单 Agent 单连接顶替且静默不闪断）；瞬时断开泄漏、断线幽灵实体等 P0 级缺陷均已修复。
- **热可调**：推送档位、速度上限、观察采样率、`max_agents` 后台改 60s 内生效；在线连接改档还能即时生效。
- **跨世界身份**：RS256 handoff + **nonce 一次性消费**（顺带补上人类链路"nonce 生成但从不校验"的历史缺口）+ transient session **不建本地 user/character**（users/characters 行数前后不变）。
- **长驻可用**：Key 档自动续期（`sessionTtl×2/3` 换票，WS 不重连），修掉了"15 分钟后 AI 还在说话却看不见世界"的静默失明。
- **开发者友好**：OpenAPI 与实际实现逐项核对（不写未实现端点）、README 八动作与续期说明、三步跑通示例客户端。

**指标**：游客 75/75、Key 38/38、多端 25/25；**100 游客 Agent + 3 Key + 2 真人在场 60s**：CPU 0.079 核、RSS 92MB、observe P50 7ms / P95 14ms / 0 个 429、真人端真 GPU 60FPS、0 console error；realtime 实测 9.0Hz/实体、4.5KB/s；游客连接同窗口 0 消息 0 字节（红线反证）。

**配套**：聊天记录双保存（本地 `world_chat_log` + 每日 JSONL→gzip→S3 兼容归档，**未上传成功永不删本地**）；日志三分流（access/ops/audit 按天轮转）；物体 AI 描述（上传即带 + 编辑器补填 + `observe` 可读）。

### 3.6 卡顿治理

**现役做法**：GLB 解析移入 Worker（transferable 序列化、纹理 OffscreenCanvas 预降、静态模式烘焙）；全图占位场（1 draw call 覆盖 1000+ 对象，250ms 渐缩退场）+ **预热预算**（未预热 mesh 不入屏，编译后按 170ms/帧 逐个 `getUniforms()` 把 ANGLE/D3D11 的着色器反射成本摊薄——把"画面死住"换成"模型稍晚出现"）；**点光灯池**（12 盏常驻、租用归还，消除灯数变化引发的全场景重编译）；几何合批；表面碰撞（three-mesh-bvh 胶囊 + 可踏面 60° 判据 + 撞墙回滚与单轴滑动）；加载进度引擎（分母=兴趣集，0.7 下载字节 + 0.3 确认上屏，迟到任务代际隔离）；后台标签页节流。

**技术优势**

- **把不可控的驱动行为变成可预算资源**：D3D 同步着色器反射 160~340ms/程序且不可中断，改成每帧只结算 N 个。
- **诊断方法论比修复本身更值钱**：`visible=false` 在本项目**不可作为成本判据**（3 个模块每帧接管可见性），权威口径是 `onBeforeRender` 逐对象计数；已固化成诊断三件套（`diagTurnLag` / `diagRenderCost` / `diagTurnSweep`）。
- **观感与性能不冲突**：全图占位场让"远看有东西、近看是真模型"同时成立。

**指标**：密集区 draw calls 3064→837（**−73%**）；灯数变化冻结 **8989ms → 0 尖峰**；出生点首进 worst 386ms、>500ms 长帧 0；解析 23/23 走 Worker 且渲染完整；占位场全图 1071/1071 覆盖、draw call 2150→956（−56%）。

### 3.7 GLB 资产手术工具链

**做法**：`textureCompress` / `glbTextureStripper` / `glbFaceReducer` / `modelDecimate` + 一批运维脚本。核心是"用 JSON + BIN 手术改写 GLB，不损失几何"，并已产品化到上传管线（自动套用）。

**技术优势**

- **按用途分级压缩**：法线/遮蔽无损重编码，baseColor 调色板量化——不动几何、画质可像素级验证（PSNR 31.4dB、差异像素 0.35%）。
- **难点已产品化**：bufferView 重排、accessor 索引重映射、meshopt 扩展 byteOffset 平移、多 buffer 布局，全部有**五重闸门**守护（mesh 名/压缩/多 buffer/几何一致性/回读验证），失败宁可跳过不落盘。
- **面数绝对保证**：让"100 面低模"成为可承诺的规格。

**指标**：课桌 25.74MB→8.22MB（**−68.1%**）；20.7MB 多 buffer 4K 模型→4.38MB（**−92.5%**）；LOD 变体贴图剥离 215 件 / 0 失败。

### 3.8 骨骼与动画兼容套件

**做法**：`animConventionCompensator`（约定补偿 + 容器旋转下沉归一）、`duplicateBoneChainFixer`（`_N` 副本链换绑回主链并同步 IBM，带世界矩阵门控）、`bone-physics` 词边界正则、`animUpAxisFixer`、多平台骨骼适配（Mixamo / RPM / VRoid / RootMotion）。

**技术优势**：别人导出的模型/动画**进世界就能用**，不需要逐个人工修——Blender Z-up 残留容器旋转自动归一、动画约定不一致自动补偿、重复骨骼链自动换绑；每层都有安全门（补偿后 rest 仍异常则拒补偿并告警；换绑后 IBM 变差立即回滚）；`_diag` 诊断接口既自检也可派给自动化验收。

**指标**：轨道命中 33/33、312/318；Kipfel 类 51 骨补偿逐模型 10/10；骨骼套件回归 67/67。

### 3.9 实时通信与多人体验健壮性

**做法**：`wsPresenceGuard`（缓存 PLAYER_JOIN 在每次重连后自动重发 + 无限重连 + 应用层 PING/PONG 看门狗 + 服务器未登记连接告警）；附近语音 PTT 半双工 + 30m 中继 + 距离衰减 + **同时说话人数名额制**；全链路回收（视频 DOM、灯池、气泡、僵尸连接）。

**技术优势**

- **空间感不再"靠刷新恢复"**：AB 双方各自掉线后各自自愈，不靠人手动刷新页面。
- **成本可预期**：语音从"30m 内人多就整区禁言"改为"同时说话人数上限"，上限还能按服务器规格在后台选（10 人≈1.3Mbps/1核2G，20 人≈2.6Mbps/2核4G）。
- **合规最优**：语音链路服务端**零加工**（不做 ASR/TTS），转写归属 AI 客户端。

**指标**：重连存在感 9/9 ACCEPTED（重连后对端仍能看到我移动）；内存审计四项泄漏修复。

### 3.10 跨世界联邦传送

**技术优势**

- **同名冲突不再把玩家丢成游客**：目标世界按 email 复用账号，否则候选昵称逐个探测 + 唯一键冲突自动改名重试（旧实现 500 → 前端弹登录框 → 玩家变游客）。
- **过设计 vs 最小实现的教训已固化**：https 页面回程加载 http 世界资产被浏览器硬拦，最终只用"协议表头修正"1 个纯函数 + 3 处加载口各 1 行解决；曾做过的 200 行 URL 归一化模块会把路径形态改坏导致**所有玩家**加载失败，已全部撤销。
- **反代后限流口径正确**：`X-Real-IP` / XFF 最后一段，不会被"全世界算成一个 IP"打爆。
- **架构约束文档化**：联邦 CORS 全开是设计必需（不是漏洞），避免后来人误修而弄瘫联邦。

**指标**：同名不同邮箱 4/4 PASS；协议修正纯函数 15/15 + 浏览器 E2E 10/10 + 主世界冒烟 9/9。

### 3.11 世界的 AI 可读性与资产治理

物体语义描述全链路（上传弹窗 / 编辑器面板 / 后端 PUT / `observe` 可读 → 9/9）；上传元数据对话框；编辑器懒加载；孤儿资产清理（159 个孤儿 / 2.9GB 删除，目录 3.1GB→390MB）。

**优势**：AI 认识"这是什么"而不是只看 `name` 和坐标；资产库干净直接降低跨世界首屏成本。

---

## 第 4 章 贯穿性工程能力

> 这些不是功能，但比任何单个功能更值钱——它们决定了"能不能长期做下去"。

1. **验收脚本化、可重跑**：仓库里 **54 个 `accept_*.js`**，判据 = 真实退出码 + JSON 报告 + `VERDICT ACCEPTED`。任何一次改动都能回归（LOD 23/23、Agent 75/75、3DGS 26/26…）。
2. **硬约束换来的可维护性**：单文件 ≤500 行（>1000 行禁止追加）、黑名单大文件零追加、新功能独立模块、前端 `?v=` 递增、临时脚本 `_tmp_` 用完删——这是 AI 协作能连续几个月不烂尾的制度保障。
3. **量化归因方法论**：不用"可见性"当成本判据、不用包围盒判蒙皮姿态、像素对比（PSNR/diff%）判资产是否等价。
4. **文档接力**：每个大功能一份"唯一权威文档"（进度表 + 红线 + 代码坐标速查 + 已知坑），新会话读进度表即可接上。

---

## 第 5 章 现状说明与后续机会

### 5.1 准确起见：以下内容目前**不在**工作区（写对外材料时不要引用）

| 项目 | 现状 |
|---|---|
| `prewarmGate.js` / `compileBudget.js` | 已不在（能力并入 `placeholderField.js` 的 170ms/帧预热预算） |
| `skeletonIntegrity.js` | 已不在（`duplicateBoneChainFixer` / `animConventionCompensator` 在，功能大体覆盖） |
| 玩家三档分级渲染（`playerUnloadManager.js` / `blockCharacterFactory.js`） | 已不在，需重建 |
| `player.js` 的"帧率解耦" | **当前仍是"每帧固定增量"（`player.js?v=8`），低帧率下移动/转向会变慢** —— 目前代码里一个真实短板 |
| `Three.js-r185-升级规划与规范.md`、`特效系统-开发规范与进度.md` | 当前不在工作区 |

另：`ubuntu-deploy-package` 不在本工作区（多个功能待同步）；GLB 改动生效需强刷（无版本号的 GLB 走 30 天 immutable 缓存）。

### 5.2 已经被量化、只差执行的机会

- **角色模板库瘦身**：角色模板库 205.5MB 里有 **79.3MB 是"删动画只改 JSON、BIN 没重建"的孤儿数据**（集中在 4 个文件，最严重一件 93.4% 是垃圾）。清理后跨世界首屏可从几十秒降到 1~2 秒。方案已定，未实施。
- **恢复第 5.1 节丢失模块**：优先恢复 `player.js` 帧率解耦（低帧率手感）与玩家三档分级渲染（人多时的性能分级）。

---

## 附：可复现命令（对内）

```bash
# LOD 相关
node scripts/accept_lod_stage5.js
node scripts/accept_lod_stage6_texture_share.js
node scripts/accept_lod_stage7_standalone.js
node scripts/accept_lod_stage7b_lifecycle.js

# r185 升级
node scripts/accept_stage1_world.js
node scripts/accept_stage2_shims.js
node scripts/accept_stage3_editors.js
node scripts/accept_stage5_3dgs.js
node scripts/smoke_r185_world.js

# AI Agent
node scripts/accept_agent_p1.js
node scripts/accept_agent_p8.js
node scripts/accept_agent_capacity.js
node scripts/accept_agent_longsession.js

# 卡顿治理 / 加载
node scripts/accept_worker_parse.js
node scripts/accept_turn_freeze_fix.js
node scripts/accept_placeholder_field.js
node scripts/accept_ws_reconnect_presence.js
```
