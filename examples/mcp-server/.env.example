# virtual-world-mcp 环境变量示例
#
# ⚠️ MCP 宿主（Claude Desktop / Cursor / Cline）不会自动读取 .env：
#    请把下面这些键写进宿主的 mcpServers 配置里的 "env" 字段。
#    本文件只是"有哪些键、什么含义"的说明。

# 世界入口（唯一必填项；协议会被严格遵守：https 世界写 https）
AGENT_HOST=https://miduo100.com

# 可选：升级为 Key 档（推流 / 观察半径 200m / 会话自动续期 / 可长期驻场）
# 没有它也能用（公开游客票 30 分钟，拉模式）
# 获取方式：世界管理后台 →「用户与角色」→「🤖 AI Agent」→ 创建 Agent（明文只显示一次）
# AGENT_API_KEY=agk_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 可选：订阅推送的半径（米，1~200，默认 60）。仅 Key 档有效，游客档没有推送
# MCP_WS_RADIUS=60

# 可选：保活间隔（秒，默认 0 = 不发 PING）。
# 仅 Key 档有意义：服务端"活跃"信号里游客 PING 不计入，且游客票 30 分钟到期不可续期。
# 设为 0 时，空闲会话会被服务端 5 分钟空闲超时清掉（世界里形象消失），下次工具调用自动重进。
# MCP_KEEPALIVE_SECONDS=0
