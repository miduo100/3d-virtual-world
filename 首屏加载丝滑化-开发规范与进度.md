# 首屏加载丝滑化 — 开发规范与进度

| 项 | 值 |
|---|---|
| 项目 | `l:\shegnjir185`（虚拟世界 / Express + PostgreSQL + Three.js r185） |
| 文档性质 | **唯一权威开发文档**（接力入口）：规范 + 规格 + 分步任务 + 进度表 |
| 版本 / 日期 | v1 / 2026-09-24（代码核对基准日） |
| 本轮范围 | **C 档**：环境就绪闸门 + 天空盒让路 + 玩家模型调度（含距离过滤）+ **同模板多玩家去重** |
| 明确不做 | 统一资产调度器（第二层）、LOD 改造、场景队列策略改造、服务端/DB 改造、渐进式"先低模后高模"加载 |
| 接力方式 | 新会话读本文档 → 按 §0 开工 → 每完成一步更新 §12 进度表 |
| 前身文档 | `首屏加载丝滑化-实现提示词-v1.md`（已被本文档取代并删除，避免两份口径不一致） |

> **⚠️ 行号会漂移**：本次核对发现工作区有 30+ 未提交改动，`world.js` 已从 `?v=12` 变为 `?v=13`、
> `loadModelWithRealProgress` 偏移 +28 行、`websocket.js` 的 `handlePlayerJoined` 偏移 +4 行。
> 因此本文档**所有定位一律使用"锚点字符串"（§9），行号仅作导航参考，动手前必须用锚点重新定位。**

---

## §0 执行指令（给接手 AI）

### 0.0 开工前置（先做，别跳）

- [ ] 0.0.1 `git status --short` 查看工作区：当前有 30+ 未提交改动（AI Agent 引流/MCP/SEO 等），其中 **`public/js/world.js`、`public/js/websocket.js`、`public/index.html`、`public/js/ui.js`、`public/js/chatBubbles.js` 均处于 modified**。**先问用户**：这些改动是"先提交后再开工"还是"与本任务共用一次提交"？避免收尾时无法分离。
- [ ] 0.0.2 确认 3 个新文件尚不存在（已核对：`worldReadyGate.js` / `playerModelScheduler.js` / `gltfTemplateCache.js` 均不存在，无冲突）。
- [ ] 0.0.3 确认 vendor 来源存在：`node_modules/three/examples/jsm/utils/SkeletonUtils.js`（11.75KB，已核对）。
- [ ] 0.0.4 确认 `public/js/lib/three-examples/` 目前只有 `curves/ libs/ loaders/`，**没有 `utils/`**，需新建目录。

### 0.1 口径确认（开工前必须问用户）

| 编号 | 事项 | 建议默认 |
|---|---|---|
| **A** | "OD" 是否 = LOD（三版模型方案/分带/变体/合批）？ | 按"是"处理 → LOD 全套**零改动**；若有其他含义先澄清 |
| **B** | 是否允许加"**让路包装**"（包装 `World.prototype.processLoadingQueue`，玩家模型档在途时本轮不启动 >15MB 场景对象；仅包装，不改 `world.js` 文件） | **建议加**；否则"自己角色 ≤ 就绪+8s"在慢网下不稳定 |
| **C** | 就绪判据用**实用口径**（不含"场景大 GLB 全部完成"） | **按实用口径**（§4）。若要求"队列全空才就绪"，自己要等到实测 48s 档位才出模型 |
| **D** | 纳入"URL 协议无关归一化"+"跨域守卫结果按 key 复用一次" | **建议纳入**（§7.4），成本极小但决定跨世界能否真正省到解析与显存 |

### 0.2 工作方式

- [ ] 按 **Step 1 → Step 2 → Step 3** 顺序推进，**每步先验收通过、经用户确认，再进下一步**（禁止跳步）。
- [ ] 每步完成后立刻更新 §12 进度表。
- [ ] 中文提交信息必须走 UTF-8 文件 + `git commit -F`（PowerShell 直传中文会乱码）。

### 0.3 硬性禁止（违反即失败）

1. **禁止在 `public/js/world.js` 追加任何代码**（大文件黑名单）；对它的改造只能"包装原型"。
2. 禁止修改 `src/routes/federation.js`、`src/routes/admin.js`、`geometryBuilder.js` 等大文件。
3. 禁止改动 LOD 全套、几何体/占位符机制、场景队列排序与串行策略（除 §0.1-B 被批准）。
4. 禁止改数据库结构、服务端接口、联邦逻辑。
5. 新功能必须独立文件（理想 ≤500 行，绝不超 1000 行）。
6. 不留半成品。

---

## §1 背景：限速实测基线（2026-09-22 实跑，新会话无此上下文）

### 1.1 测试条件

| 项 | 值 |
|---|---|
| 环境 | 本地 `localhost:3002` + Chrome headless（真 GPU）+ CDP `Network.emulateNetworkConditions` |
| 限速 | 6 Mbps / RTT 200 ms（**观测实际吞吐 ≈2 MB/s**，比真实服务器宽松，真实环境同比例放大） |
| 被测角色模板 | `Kipfel_Mobile` = `/uploads/character-templates/char-1788236817301-186541966.glb`（29.3MB，8 动画） |
| 对照实验 | 同条件但 localStorage 不含角色模板（不加载角色模型） |

### 1.2 实测时间线（现状）

| t | 事件 | 归属 |
|---|---|---|
| 4.5~5.3s | `/api/users/character`、`/api/public/character-templates`、`/api/world/state`、`/api/auth/me` | 加载屏阶段 |
| **5.2 → 19.3s** | **自己的角色 GLB 29.3MB（14.1s）** | 抢跑（加载屏期间即发起） |
| **6.1 → 50.3s** | **天空盒 HDR EXR 11.75MB（44.1s）** | 抢跑（`new World()` 即发起） |
| 9.4 → 27.5s | 媒体：图片 0.19/0.49MB、视频 5.16MB（最终 abort） | 独立媒体通道 |
| 9s | 1075 个占位方块就位（1 draw call） | 受控 |
| **10 → 12s** | **几何体 365 个全部上屏**（本地生成、零网络） | 受控且高效 |
| **12.1 → 28.2s** | 场景 GLB #1 4.08MB（14.7s，含一次 `HEAD`） | 串行队列 |
| 19.4 → 22.1s | 8 个角色动画（模型就绪后补载） | 抢跑 + 被模型阻塞 |
| 21s / 22s | 角色模型上屏 / 8 动画绑定完成 | — |
| 29.8 → 47.5s | 场景 GLB #2 5.78MB（17.6s） | 串行队列 |
| 30 → 48s | 场景 GLB #3 6MB | 串行队列 |
| **48s** | 场景队列清空 | — |

### 1.3 关键归因（对照实验）

去掉角色模型后：场景 GLB #1 `14.1s`（原 14.7s）、#2 `17.5s`（原 17.6s）、队列清空 `46s`（原 48s）。

> **只快约 2 秒 ⇒ 代码里不存在"等角色模型加载完再加载场景"的依赖。**
> 真实耗时 = ① 多个大文件（29.3MB 角色 + 11.75MB 天空 + 5MB 媒体）**并发抢同一条链路**；
> ② 场景 GLB **严格串行**（一次 1 个）；③ 场景大对象被"大文件阻塞规则"**主动压后**。
> 用户观感"等角色模型"来自真实的先后顺序（角色 19~21s 完成，场景 GLB 26~48s 逐个出现）。

### 1.4 掉帧证据（"丝滑"问题的量化依据）

| t | fps | 同帧叠加的工作 |
|---|---|---|
| 13s | 8 | 几何体批量上屏 + reveal 预热 + 纹理降级 |
| 21s | 13 → 0 | 角色模型上屏 + 骨骼补偿 + 碰撞/BVH |
| 22s | 3 | 8 个动画绑定 + 共享 mixer 建立 |
| 全程 | longtask 24 次 | — |

### 1.5 现状结构：受控 1 类 vs 抢跑 7 类

**受控（唯一排队部分）**

| 环节 | 锚点 |
|---|---|
| 清单拉取（`new World()` 后 1s 定时器） | `setTimeout` → `loadGeneratedBuildings`（`world.js`，约 4838 行）→ `WorldSpatialManager.initialLoad` |
| 优先级排序 | 几何体+媒体第一优先；GLB 按 `<5MB / 5-15MB / >15MB` 分层；同层 `LoadFacing.sector` 面向优先 |
| 按距离入队 | `updateObjectLoading()`（`animate` 每 30 帧触发） |
| 消费 | `processLoadingQueue()`：`shouldBlockLargeFile` 阻塞、`adjustedBatchSize = 1` 串行 |
| 距离口径 | `loadDistance = 200`、`unloadDistance = 400`、队列上限 200、`LARGE_MODEL_THRESHOLD = 15MB` |

**抢跑（fire-and-forget，绕过队列、无优先级、无并发上限）**

| # | 资产 | 触发锚点 |
|---|---|---|
| 1 | 天空盒 HDR EXR | `world.js` 的 `setupScene` → `SkyManager.fetchFallback()`（`skyManager.js`，约 197 行，3s 后拉天气配置） |
| 2 | 自己的角色模型 | `main.js`：`player = new Player(...)` → `player.js` 构造函数 → `world.addPlayer(...)` → `_loadPlayerGlb` |
| 3 | 自己的角色动画 ×8 | `main.js` 的 `scheduleLoadAnims`；模型未就绪时 500ms×20 重试（`_loadPlayerAnimGlb` 内），10s 后转 `_pendingAnimUrls` |
| 4 | **其他玩家模型（4 条路径）** | ①`main.js` 的 `worldState.characters.forEach`（**加载屏隐藏前**）②`websocket.js` 的 `handleWorldState` ③`handlePlayerJoined`（内部 `addPlayer`）④`handleModelUpdate`（内部 `_loadPlayerGlb`） |
| 5 | 其他玩家动画 ×N×8 | `websocket.js` 的 `_loadRemotePlayerAnims` |
| 6 | 媒体（图片/视频） | `world.js` 的媒体独立通道（`updateObjectLoading` 内） |
| 7 | 模板音效 | `main.js` 的 `soundManager.loadTemplateAudio` → `preload`（全并发） |

**两个决定性事实**

1. `_loadPlayerGlb` 内每次 **`const _charLoader = new THREE.GLTFLoader();`**（注释就在其上方：曾因共享 `gltf.scene` 导致"模型被抢走"而改为完全不共享）⇒ **同模板 N 人 = N 次下载 + N 次主线程解析 + N 份显存**。
2. 角色模型**不走 Worker**（场景模型走 `GltfWorkerClient`）⇒ 解析全压主线程，是"模型一出现就卡"的主因。

---

## §2 范围与已拍板决策

### 2.1 范围恰好 5 项

| # | 项目 | 本轮做什么 | 是否消费就绪信号 |
|---|---|---|---|
| 1 | 天空盒 | 进世界即下 11.75MB EXR → **先程序化渐变占位，就绪后**才下高清并替换（PMREM 环境光一并推迟） | ✅ |
| 2 | 自己的角色模型 | 就绪后才发起；动画随模型就绪立即加载（不再 10s 空转） | ✅ |
| 3 | 其他玩家模型 | 就绪后才加载，**只加载视野内最近的**，并发 1，离队取消排队 | ✅ |
| 4 | 同模板多玩家复用 | 同 URL **只下载+解析一次**，其余克隆复用（解析与显存 N → 1） | ❌（挂在放行后的加载路径） |
| 5 | 环境就绪信号 | 新建统一闸门 `worldReadyGate.js` | 它是**提供者** |

> 验收按"**3 项资产 + 1 个信号 + 1 个去重机制**"分开断言，不要笼统写成"5 项都响应就绪信号"。

### 2.2 已拍板决策（不得擅自更改）

| 项 | 决策 |
|---|---|
| 加载秩序 | **环境（视野内场景）→ 占位（蓝盒子 + 几何方块人）→ 环境无物可加载后 → 自己的角色模型 → 其他玩家模型** |
| 其他玩家 | **默认几何方块人（棍人）替身，按需替换**；不重建替身工厂 |
| 天空盒 | **延迟 + 降级替换**（低清用程序化渐变，不新增图片资源、不改服务端） |
| 实施范围 | 第一层三件套 + 同模板去重（C 档）；统一资产调度器本轮不做 |

**替身现状说明（省事关键）**：`blockCharacterFactory.js` / `playerUnloadManager.js` / `adminPlayerLod.js` **不在磁盘上**（历史丢失）。不需要重建：`addPlayer` 天然先拼 9 个几何 mesh（方块身体 + 球头 + 圆柱四肢），这就是现成的"几何方块人替身"。所以"默认替身 + 按需替换"只需**不立即发起模型加载**。

---

## §3 目标加载秩序（验收画面基准）

| 阶段 | 系统在做什么 | 用户能看到什么 | 网络负载 | 本轮 |
|---|---|---|---|---|
| 0 进入中 | 加载屏只覆盖"必须阻塞部分"（API 元数据 + WS），满足即隐藏 | 全屏转圈（`#loadingScreen` 仅 spinner） | API 元数据 | 不改逻辑（因他人模型不再抢带宽而更快隐藏） |
| 1 骨架瞬间 | 渲染循环启动；清单回后为**全图**对象摆占位符（1 draw call） | 青蓝半透明盒（5×6×5、抬高 3m）+ 自己是几何方块人，**已可移动** | 极低 | 不改 |
| 2 环境骨架 | 几何体（本地、零网络）批量上屏；`reveal` 预热后占位盒 250ms 渐缩 | 盒子"闪一下"变真身；近处/面向优先 | ≈0 | 不改 |
| 3 小→中→大 | 场景 GLB 分层串行（一次 1 个） | 远处/大件仍是蓝盒子，逐个变真 | 串行，单对象 4~6MB | 不改（除 §0.1-B 批准时加让路） |
| 4 **自己角色** | 就绪闸门触发后发起模型 + 8 动画 | 方块人 → 真模型 + 待机动画 | 角色档独占（并发 1） | 🔧 |
| 5 **其他玩家** | 就绪 + 自己之后；视野内最近优先，并发 1，离队取消 | 别人先是方块人，逐个替换 | 每玩家 10~40MB | 🔧 |
| 6 天空/媒体 | 天空：渐变占位 → 就绪后换高清；媒体保持现状 | 天空氛围先有、后变高清 | EXR 11.75MB | 🔧（仅天空） |
| 7 稳态 | LOD 分带（默认 30/60/400，后台可调）与合批按距离生效 | 走近清晰、走远变简/变盒 | 变体懒加载（低模 ≤100 面） | **零改动** |

---

## §4 就绪闸门规格（`public/js/worldReadyGate.js`，新增）

**导出**：`window.WorldReadyGate = { isReady(), onReady(cb), once(cb), reset(), _diag() }`

**判据（实用口径，常量可调）**

```
ready = (几何体对象全部已上屏) && (loadingQueue 中已无 < 15MB 的对象)
        || sinceEnter >= 12000ms        // 硬兜底，防某些世界永不就绪
```

**数据来源（只读）**：`gameWorld.allWorldObjects` / `loadedObjects` / `loadingQueue` / `loadingBatch`；几何体判据 `obj.type.startsWith('geometry_')`；可叠加 `LoadingProgress._diag()` 交叉校验。

**`_diag()` 必返字段**：`{ ready, reason:'ready'|'timeout', tReadyMs, sinceEnterMs, geometryDone, pendingSmallMedium, queueLen, batchLen }`

**禁止**：任何写操作；重量级轮询（建议 500ms 轮询或复用帧循环，接入 `BgThrottle`）。

---

## §5 改动清单

### 5.1 新增文件

| 文件 | 预算 | 作用 |
|---|---|---|
| `public/js/worldReadyGate.js` | ~130 行 | 环境就绪闸门（§4） |
| `public/js/playerModelScheduler.js` | ~300 行 | 玩家模型/动画调度（§6） |
| `public/js/gltfTemplateCache.js` | ~180 行 | 同模板源缓存 + 克隆（§7） |
| `public/js/lib/three-examples/utils/SkeletonUtils.js` | vendor | 来源 `node_modules/three/examples/jsm/utils/SkeletonUtils.js`（需新建 `utils/` 目录） |

### 5.2 修改文件

| 文件 | 版本号 | 改动 |
|---|---|---|
| `public/js/skyManager.js` | `?v=1` → `?v=2` | 延迟 + 渐变占位 + 就绪后替换 + 环境光推迟（§8） |
| `public/index.html` | 3 行引入 | 插入位置：`js/worldSpatialManager.js?v=1` 之后、`js/player.js?v=8` 之前（当前约 1526~1539 行；**必须先读文件确认，不可按行号写死**） |

> 当前 `index.html` 版本号快照（核对于 2026-09-24）：`skyManager.js?v=1`、`world.js?v=13`、`worldLodStandalone.js?v=5`、`worldSpatialManager.js?v=1`、`player.js?v=8`、`websocket.js?v=10`、`main.js?v=3`。**`world.js` 已完成一次版本递增（12→13），说明它近期被改过——再次强调用锚点定位。**

### 5.3 零改动保护清单（违反即失败）

| 类别 | 对象 |
|---|---|
| 黑名单大文件 | `public/js/world.js`（只包装原型）、`src/routes/federation.js`、`src/routes/admin.js`、`geometryBuilder.js` |
| LOD 全套 | `worldInstanceMerger_v2.js`、`worldLodAssets.js`、`worldLodStandalone.js`、`worldObjectBounds.js`、`modelLod.js`、`glbFaceReducer.js` |
| 几何体与占位符 | `placeholderField.js`、`geometryBatcher.js`、`geometryBuilder.js` |
| 场景队列策略 | 排序 / 串行 / 大文件阻塞规则（除 §0.1-B 批准） |
| 进度条 | `loadingProgress.js`（只读 `_diag()`） |
| 服务端 | 零改动（天空低清用程序化生成） |

---

## §6 玩家模型调度规格（`public/js/playerModelScheduler.js`）

**做法**：包装原型（照 `worldTextureOptimizer.js` 既有模式：先保存原函数引用再替换）。**必须覆盖 2 个入口**：

| 包装对象 | 覆盖的调用点 |
|---|---|
| `World.prototype._loadPlayerGlb` | `main.js`（首载 + REST 补载）、`websocket.js`（`handleModelUpdate` / `handleWorldState` / `handlePlayerJoined` 内部）、`world.js` 的 `addPlayer` 尾部 |
| `World.prototype._loadPlayerAnimGlb` | `main.js` 的 `scheduleLoadAnims`、`websocket.js` 的 `_loadRemotePlayerAnims` |

**分档放行**

| 档位 | 判定 | 放行条件 |
|---|---|---|
| P2 自己 | `characterId === GAME_STATE.characterId`（与 `world.js` 内 `const isSelf = ...` 同判据） | `onReady()` 后立即放行 |
| P3 远端 | 其他所有玩家 | `onReady()` 后做"视野内"过滤：距离 ≤60m 且（视锥内 或 ≤25m）；每 2s 重评（`BgThrottle`）；按距离升序；**并发 1** |
| 动画 | 挂在对应玩家上 | 其模型就绪后**立即**放行（不得再出现 500ms×20 空转重试） |

**5 条硬性要求**

1. **放行前绝不能写 `characterGroup.userData._loadingGlbUrl`**——`world.js` 用它做幂等（`if (characterGroup.userData._loadingGlbUrl === url) { ... return; }`），提前写会让真加载被判"重复加载中"而跳过。
2. 按 `(characterId + 归一化 url)` 去重；URL 变化取消旧任务重排（`main.js` 会二次调用 `_loadPlayerGlb`）。
3. 离队/离开视野：**排队中**直接出队；**已发起**标记 stale，完成时**不解析、不上屏**（物理限制：`GLTFLoader.load` 无法 abort 网络，字节仍进 HTTP 缓存，下次秒回）。
4. 兜底：`onReady` 未在 12s 内触发则按 `timeout` 强制放行自己（远端仍走视野过滤）。
5. `_diag()` 暴露：`{ ready, selfState, remoteQueued[], remoteActive, canceled, blocks }`。

**禁止**：改动 `world.js` 状态；修改加载队列排序。

---

## §7 同模板多玩家复用规格（`public/js/gltfTemplateCache.js`）

### 7.1 定位（别搞错优化对象）

**不是"重复浏览器缓存"**。浏览器只省"字节"，不省"解析"与"显存"：

| 成本层 | 同 URL 第二次 | 谁负责 |
|---|---|---|
| 网络字节 | ✅ 免费（`max-age=2592000, immutable`） | 浏览器 HTTP 缓存，**我们不去优化** |
| 解析（主线程） | ❌ 每次重跑 `GLTFLoader` | **我们** |
| GPU 显存 | ❌ 每次新建 Texture/Geometry | **我们** |

> 关键认知：**"同一个文件"≠"同一个可用对象"**。浏览器缓存字节流；程序要用的是 `Object3D`（含 `Skeleton`/`Bone`/`Texture`/`BufferGeometry`），必须重建。

### 7.2 共享范围（只共享"源"）

| 共享（按 URL 缓存） | 每位玩家各自做 |
|---|---|
| 源 `scene`、源 `AnimationClip[]` | `fitModel`、骨骼平台适配、校准参数、武器挂载、动画绑定、材质隔离 |

原因：每人 `boneMapConfig` / `weaponSocketConfig` / `calibrationConfig` 不同（自己来自 localStorage、远端来自 WS）。

### 7.3 必踩坑（逐条处理）

1. **蒙皮克隆必须 `SkeletonUtils.clone()`**，禁止 `Object3D.clone()`（后者共享骨骼 → 多人动画互扰）。
2. **每个玩家的动画 clip 必须克隆**（`track.values` 深拷贝）——`animConventionCompensator` **就地改 `track.values`**（按 `bindCache` 缓存），共享 clip 会出现"第二个人进来后第一个人动画错乱"。
3. 材质默认共享引用（clone 天然共享 `geometry/material/texture`，显存才降得下来）；**会被改的材质**（受击闪红/换色）必须"clone 材质 + 共享纹理"。
4. **去重键 = 协议无关指纹**：`hostname + pathname`（忽略 scheme 与 query）。项目已知 http/https 前缀分裂（`assetProtocolFix.js` 就是为 Mixed Content 修的），不归一化会白解析两次。
5. **LRU 上限 8~16 个 key**；淘汰时若仍有实例引用则标记 dead、引用归零再释放结构；**禁止直接 dispose 共享 geometry/texture**。
6. **不做 per-世界的纹理降级**（玩家模型路径本就不走 `worldTextureOptimizer`；将来若接，降级档位必须并入 key）。
7. `_diag()` 必返可断言数字：`{ hitCount, missCount, parseCount, sharedInstances, estimatedSavedParses, keys[] }`。

### 7.4 跨世界与守卫（对应 §0.1-D）

- **守卫复用**：`RemoteModelGuard`（`world.js` 内 `if (!isSelf && window.RemoteModelGuard && !_isSameOrigin && !_relaxMg)`，仅非同源生效）内部会先拉服务端配置；必须**按 key 校验一次、结果复用**。
- **命中范围矩阵**

| 场景 | URL 情况 | 字节层 | 解析/显存层 |
|---|---|---|---|
| 同世界同模板 | 同一 URL | ✅ 免费 | ✅ 1 次 / 1 份 |
| 跨世界·同一源世界同模板 | 同一绝对 URL | ✅ 免费 | ✅ 1 次 / 1 份 |
| 跨世界·同一源世界但 http/https 前缀不同 | 归一化后相同 | ⚠️ 可能两份 | ✅ 1 次 / 1 份 |
| 跨世界·**不同源世界**同模板 | URL 域名不同 | ❌ 各下各的 | ❌ 各解析各的（物理上是两份文件，**不要合并**） |
| 页面 https ← 源世界只有 http | Mixed Content 被拦 | ❌ 取不到（停在方块人） | 不适用 |

---

## §8 天空盒让路规格（改 `skyManager.js`，`?v=1` → `?v=2`）

只在 `skyManager.js` 内改（`world.js` 的 `setupScene` 调用点保持不变）：

1. `applyConfig(sky, weatherType)` 内：若 `sky.kind === 'hdr'`（或体量大的全景）且 `WorldReadyGate` 未就绪 → 存 `state.deferredSky`，立即用**程序化 `CanvasTexture` 渐变**顶上（`mapping = THREE.EquirectangularReflectionMapping`，零网络、<5ms），并保持 `state.sky` 防重复触发。
2. `onReady`（或 20s 兜底）→ 走原 `loadTexture()` + `paint()` 替换；**必须复用** `state.loadToken` 机制防旧选择覆盖。
3. `applyEnvironment()`（PMREM 生成，主线程开销）与高清纹理**一起推迟**。
4. 失败必须回退纯色 + `console.warn`（沿用"绝不静默失败"约定）。
5. `_diag()` 增加：`{ deferred, deferredSince, placeholderKind:'gradient'|'none' }`。

---

## §9 锚点定位表（**动手前用它重新定位，不要依赖行号**）

> 行号核对于 2026-09-24，仅供导航；工作区有 30+ 未提交改动，随时可能漂移。

### 9.1 `public/js/world.js`

| 用途 | 锚点字符串 | 核对行号 |
|---|---|---|
| 加载/卸载距离 | `this.loadDistance = 200;` / `this.unloadDistance = 400;` | 118 / 119 |
| 队列上限 | `this.maxLoadingQueueSize = 200;` | 130 |
| 大模型阈值 | `this.LARGE_MODEL_THRESHOLD = 15 * 1024 * 1024;` | 199 |
| 玩家创建（方块人 9 mesh） | `addPlayer(characterId, characterName, position` | 1773 |
| 玩家模型加载入口 | `_loadPlayerGlb(characterId, characterGroup, nameSprite, glbUrl) {` | 2097 |
| 幂等锁（**放行前勿写**） | `if (characterGroup.userData._loadingGlbUrl === url)` | 2118 |
| isSelf 判据 | `const isSelf = (typeof GAME_STATE !== 'undefined' && characterId === GAME_STATE.characterId);` | 2129 |
| 独立 loader（解析不共享） | `const _charLoader = new THREE.GLTFLoader();` | 2184 |
| 跨域模型守卫 | `if (!isSelf && window.RemoteModelGuard && !_isSameOrigin && !_relaxMg)` | 2222 |
| 玩家动画入口 | `_loadPlayerAnimGlb(characterId, type, animUrl, _retryCount = 0) {` | 2578 |
| 动画 pending 缓存 | `characterGroup.userData._pendingAnimUrls[type] = animUrl;` | 2593 |
| 按距离入队 | `updateObjectLoading() {` | 3211 |
| 队列消费 | `processLoadingQueue(currentTime) {` | 3470 |
| 大文件让路 | `const shouldBlockLargeFile = !nextObjIsSmall && (` | 3553 |
| 串行批次 | `adjustedBatchSize = 1;` | 3582 |
| 清单构建 | `async loadGeneratedBuildings() {` | 4838 |
| 全量对象写入 | `this.allWorldObjects = processedObjects;` | 4982 |
| 每 30 帧触发 | `if (this.frameCount % 30 === 0) {` | 7986 |
| 场景模型下载（HEAD+GET+Worker） | `async loadModelWithRealProgress(url, name, onComplete, onError, timeout = 120000) {` | 5940 |

### 9.2 `public/js/main.js` / `websocket.js` / `skyManager.js` / `index.html`

| 用途 | 锚点字符串 | 核对位置 |
|---|---|---|
| 自己角色创建 | `player = new Player(gameWorld, GAME_STATE.characterId, characterData,` | main.js 657 |
| 动画调度 | `const scheduleLoadAnims = (animUrls, effectiveGlbUrl) => {` | main.js 540 |
| 加载屏隐藏（登录/游客） | `UI.hideLoadingScreen();` | main.js 762 / 284 |
| 世界实体加载 | `async function loadWorldEntities() {` | main.js 791 |
| **他人玩家（REST 路径）** | `worldState.characters.forEach((character) => {` | main.js 812 |
| MODEL_UPDATE → 加载模型 | `static handleModelUpdate(payload) {` | websocket.js 145（内部调 `_loadPlayerGlb`） |
| 远端动画 | `static _loadRemotePlayerAnims(characterId, animUrls) {` | websocket.js 198 |
| 他人进场 | `static handlePlayerJoined(payload) {` | websocket.js 256 |
| WS 快照（他人玩家） | `static handleWorldState(payload) {` | websocket.js 407 |
| 天空配置入口 | `function applyConfig(sky, weatherType) {` | skyManager.js 151 |
| 天空纹理加载 | `function loadTexture(sky) {` | skyManager.js 114 |
| PMREM 环境光 | `function applyEnvironment() {` | skyManager.js 67 |
| 兜底拉天气 | `function fetchFallback() {` | skyManager.js 197 |
| 引入顺序锚点 | `js/skyManager.js?v=1` / `js/world.js?v=13` / `js/worldLodStandalone.js?v=5` / `js/worldSpatialManager.js?v=1` / `js/player.js?v=8` | index.html 1515 / 1516 / 1522 / 1526 / 1539 |

---

## §10 分步实施与验收（禁止跳步）

> **通用原则**：所有时间断言用**相对 `T_ready` 的差值**，不要用绝对秒数（本机基准与真实服务器带宽不同，绝对秒数必然误判）。

### Step 1 — 环境就绪闸门（只加信号，不改行为）

- [ ] 新建 `public/js/worldReadyGate.js`（§4）
- [ ] `index.html` 在 `js/worldSpatialManager.js?v=1` 之后引入（`?v=1`）
- [ ] 新建 `scripts/accept_first_load_ready_gate.js`（playwright + CDP 限速 6Mbps/200ms）
- [ ] 断言：`_diag()` 字段齐全；`tReadyMs` 落在 6~14s（本机基准）；`reason ∈ {ready, timeout}`
- [ ] 断言：构造"队列永不空"场景，验证 `timeout` 兜底可达（不卡死）
- [ ] 回归：`scripts/smoke_r185_world.js` 全绿 + 0 console error

### Step 2 — 天空盒让路

- [ ] 改 `skyManager.js`（§8）+ `index.html` 版本号 `?v=1` → `?v=2`
- [ ] 扩容 Step 1 脚本，加请求时间线采集
- [ ] 断言：`/uploads/sky/*.exr` 请求 `start ≥ T_ready`；`T_ready` 前 **0 条 EXR 请求**
- [ ] 断言：首屏仍有天空（截图采样非纯黑；允许"渐变"）
- [ ] 对比：与 §1 基线比，场景 GLB #1 完成时间应显著提前（记录数字）

### Step 3 — 同模板去重 + 玩家模型调度

- [ ] vendor `SkeletonUtils.js` 到 `public/js/lib/three-examples/utils/`（新建 `utils/`）
- [ ] 新建 `public/js/gltfTemplateCache.js`（§7）
- [ ] 新建 `public/js/playerModelScheduler.js`（§6）
- [ ] `index.html` 引入两个新文件（`?v=1`）
- [ ] 新建 `scripts/accept_first_load_player_sched.js`，覆盖 4 用例：

| 用例 | 构造 | 断言 |
|---|---|---|
| 1 单人 | 单 context 登录进世界 | 自己角色 GLB 请求 `start ≥ T_ready`；角色 ≤ `T_ready+8s` 上屏且 8 动画可用 |
| 2 双人同模板 | 两 browser context + 同一模板 | 两人模型请求都 `≥ T_ready`；`gltfTemplateCache._diag().parseCount === 1`；两人动画各自正确、互不干扰 |
| 3 视野过滤 | 远端玩家置于 200m 外 → 移到 60m 内 | 200m 外不加载（保持方块人）；移入后开始加载且并发 = 1 |
| 4 离队取消 | 排队中移除远端玩家 | 队列清空、无模型上屏 |

- [ ] **反向断言**：`0 ~ T_ready` 期间**没有**任何 `/uploads/character-templates/*.glb` 请求
- [ ] 回归：`accept_lod_stage6_texture_share.js` / `accept_lod_stage7_standalone.js` / `accept_lod_stage7b_lifecycle.js` 全绿 + 占位符相关验收 + `smoke_r185_world.js` 全绿 + 0 console error

### 10.4 量化指标（同限速基准）

| 指标 | 基线（§1） | 目标 |
|---|---|---|
| 几何体全上屏 | ≈12s | ≤3s（不变或更快） |
| 自己角色上屏 | 21s（且抢了环境带宽） | ≤ `T_ready + 8s` |
| 其他玩家模型最早发起 | 进世界即发（加载屏期间） | ≥ `T_ready` |
| 场景 GLB #1 完成 | ≈26s | ≤15s |
| 长帧（>200ms） | 掉帧点 13s/21s/22s | ≤3 次，且无"连续 2 秒 fps<30" |
| 场景队列清空 | ≈48s | ≤30s |
| 同模板 5 人解析次数 | 5 | **1** |

---

## §11 交付物与收尾

| 类别 | 内容 |
|---|---|
| 代码 | §5 清单落地 |
| 脚本 | 3 个可复跑验收脚本（PASS/FAIL 明细 + 关键数字） |
| 数据 | 改造前/后同基准对比表（`T_ready`、自己角色上屏、场景 GLB 完成、长帧数、解析次数） |
| 文档 | 新建《首屏加载流程与阶段规格.md》（阶段表 + 判据 + 命中矩阵 + 验收数字），并在既有升级/LOD 文档加一行入口 |
| 记忆 | 记录机制与坑：`SkeletonUtils`、clip 必须克隆、`_loadingGlbUrl` 幂等、协议无关 key、`GLTFLoader` 无法 abort、跨世界命中范围 |
| 同步清单 | `ubuntu-deploy-package` 待同步文件列表（本工作区无该目录，只输出清单） |
| 收尾三件事 | ①更新本文档 §12 进度表 ②写记忆 ③git 提交（UTF-8 文件 + `git commit -F`） |

---

## §12 进度表（唯一进度记录，每步完成后更新）

| 步骤 | 内容 | 状态 | 验收结论 | 完成日期 |
|---|---|---|---|---|
| 前置 | 工作区未提交改动处理方式（先提交/共用提交） | ✅ 先提交再开工（commit `368bcae2`，73 文件，2026-09-24） | — | 2026-09-24 |
| 口径 | §0.1 的 A/B/C/D 四条确认 | ✅ A=LOD 零改动 / B=加让路包装 / C=实用口径 / D=纳入归一化+守卫复用 | — | 2026-09-24 |
| Step 1 | `worldReadyGate.js` + 引入 + 验收脚本 | ✅ 完成 | accept_first_load_ready_gate.js **9/9 PASS**（tReadyMs=12931ms 走 12s 硬兜底 reason=timeout，几何体 364/364、pending=3；B 组 timeout 兜底 3716ms 可达不卡死）；smoke_r185_world.js 9/9 全绿 0 console error；基线时间线 INFO：charGlb+0ms / skyExr+702ms（Step 2 前基线） | 2026-09-24 |
| Step 2 | `skyManager.js` 让路（`?v=2`）+ 验收 | ✅ 完成 | accept_first_load_ready_gate.js（含 C 组）**15/15 PASS**：EXR 请求由 +702ms 推迟至 T_ready 后（16213ms ≥ 13205ms）、就绪前 0 条 EXR 相关请求（含 EXRLoader 模块）、就绪前渐变占位可见（avgLuma=24.7 非黑）、占位被真 HDR 完整替换（deferred=false + PMREM 环境光生效）；**场景 GLB #1 完成时间 28.2s → 10.3s（-63%）**；smoke 9/9 全绿 0 console error。A2 窗口放宽至 16s（interval tick 在限速长帧下有 ~2.5s 调度抖动，非判据错误） | 2026-09-24 |
| Step 3.1 | vendor `SkeletonUtils.js` | ✅ 完成 | 拷至 `public/js/lib/three-examples/utils/SkeletonUtils.js`；import 块改为从 `window.THREE` 解构（three-shim 未导出 AnimationMixer/SkeletonHelper，避免动共享 shim 牵连 admin.html 缓存） | 2026-09-24 |
| Step 3.2 | `gltfTemplateCache.js`（同模板去重） | ✅ 完成 | 包装 `THREE.GLTFLoader.prototype.load`，**只拦截被 register 过的 URL**（场景/武器零影响）；miss=真实加载一次+源缓存+克隆交付（源保持纯净），在途=waiters 合并；`SkeletonUtils.clone` 蒙皮换绑、`AnimationClip.clone` 深拷贝 track.values、材质 clone 共享纹理；key=`host+pathname`（忽略 scheme/query）；LRU 12 只删条目绝不 dispose；`_diag().keys[]` 含 per-key {parses,hits,shared} | 2026-09-24 |
| Step 3.3 | `playerModelScheduler.js`（调度 + 距离过滤） | ✅ 完成 | 包装 `_loadPlayerGlb`（self=onReady 即放行+13s 双保险；远端=≤60m 且（视锥内或 ≤25m）每 2s 重评、距离升序、并发 1）+ `_loadPlayerAnimGlb`（模型就绪即放行，杜绝 500ms×20 空转；**动画文件不入缓存**——§7.2 共享范围仅模型源，parseCount 口径=模型）+ `processLoadingQueue` 让路（玩家模型在途时 >15MB 队首本轮不启动）+ `modelLoaded` 事件驱动动画立即放行；放行前零写 `_loadingGlbUrl` | 2026-09-24 |
| Step 3.4 | 4 用例验收 + 回归 | ✅ 完成 | accept_first_load_player_sched.js **20/20 PASS**（U1 自己 0.76MB 模型 T_ready=12.1s 后 2.4s 上屏+8 动画全可用 idle 自动播；U2 per-key parses===1/shared=2/材质隔离/双 mixer 各归各子树；U3 200m 外 0 请求→移入 20m 加载、两远端区间不重叠、让路 blocks=3 假队首不被消费；U4 离队队列清空 0 请求；R1/R2 T_ready 前 0 条角色模板请求）。回归：smoke 9/9、ready_gate 15/15、LOD stage6 22/22、stage7 11/11、stage7b 15/15；placeholder A3c 1 FAIL 经**决定性对照**（route 屏蔽三新模块后同样 160/373 FAIL）证明与本任务无关（swiftshader+reveal 逐帧摊薄+真实在线玩家 28MB 主线程解析的环境因素；真 GPU 无限速下 373/373 全加载、blocks=0） | 2026-09-24 |
| 收尾 | 文档 + 记忆 + git 提交 + 同步清单 | ⬜ 未开始 | — | — |

> 状态取值：⬜ 未开始 / 🔄 进行中 / ✅ 完成 / ⏸ 暂停（记录原因） / ❌ 失败（记录原因）。

---

## 附录 A：本次核对发现的差异（2026-09-24）

| 项 | 旧文档记载 | 当前实际 | 影响 |
|---|---|---|---|
| `index.html` 引入段行号 | 1439~1448 / 1465 / 1467 | **1512~1526 / 1539 / 1541** | 插入点必须重新确认 |
| `world.js` 版本号 | `?v=12` | **`?v=13`** | world.js 近期被改过 |
| `websocket.js` 版本号 | `?v=9` | **`?v=10`** | 同上，`handlePlayerJoined` 偏移 +4（252→256） |
| `loadModelWithRealProgress` | 5912 行 | **5940 行（+28）** | `world.js` 在 ~4810 行之后整体下移 28 行 |
| `loadGeneratedBuildings` | 4810 | **4838** | 同上 |
| `allWorldObjects = processedObjects` | 4954 | **4982** | 同上 |
| 帧触发 `frameCount % 30` | 7958 | **7986** | 同上 |
| `_loadPlayerGlb` 区段锚点 | 2097 / 2118 / 2129 / 2184 / 2222 | **未变** ✅ | 本次要包装的函数本身未动 |
| `main.js` 全部锚点 | 657 / 762 / 791 / 812 | **未变** ✅ | — |
| `skyManager.js` 全部锚点 | 47/67/94/104/114/151/191/197 | **未变** ✅ | — |
| `three-examples/` 目录 | 含 loaders 等 | **只有 `curves/ libs/ loaders/`，无 `utils/`** | vendor 需新建 `utils/` |
| 3 个新文件 | 不存在 | **仍不存在** ✅ | 无冲突 |
| `scripts/accept_first_load_*.js` | 不存在 | **不存在** ✅ | 无冲突 |
| `SkeletonUtils.js` | 存在 | **仍存在（11.75KB）** ✅ | vendor 来源有效 |
| 工作区 | — | **30+ 未提交改动**（含 `world.js`/`websocket.js`/`index.html`/`ui.js`/`chatBubbles.js`） | 需先与用户确认提交策略 |

---

## 附录 B：实测基线原始数据（2026-09-22）

- 第一轮（含角色模型）：角色 GLB `5.2→19.3s`、天空 EXR `6.1→50.3s`、场景 GLB #1 `12.1→28.2s`、#2 `29.8→47.5s`、#3 `30→48s`、媒体视频 `9.4→27.5s`（abort）、占位符 1075 个 @9s、几何体 365 个 @10~12s、队列清空 @48s
- 对照轮（无角色模型）：天空 EXR `5.9→47.1s`、场景 GLB #1 `11.7→25.8s`、#2 `27.6→45.2s`、队列清空 @46s
- 每秒采样要点：`t=10s real=71/queue=192`、`t=12s real=365/queue=11`、`t=13s` 起 batch 内只剩 4~6MB 大 GLB 串行
- 掉帧：`13s fps=8`、`21s fps=13→0`、`22s fps=3`；longtask 累计 24

## 附录 C：常见误判提醒

| 容易被误判成 | 实际情况 |
|---|---|
| "代码里等角色模型加载完才加载场景" | 无此依赖；是抢带宽 + 串行的观感（对照实验只差 2s） |
| "浏览器缓存已覆盖同模板，无需去重" | 字节层已覆盖；**解析与显存层没有**，而后者才是多人卡顿与显存膨胀的根源 |
| "远处对象完全不渲染" | 真模型按距离（200m 入队 / 400m 卸载），但**占位盒全图都摆**（远处是蓝盒子而非空白） |
| "LOD 是几何体加载完才启用" | LOD **常驻生效**，与加载阶段无关 |
| "先加载低模再升到高模" | 现状是"占位盒 → 真模型（高模）→ 变体懒加载"；"先低后高"属二期，本轮不做 |
| "让路包装会改 world.js" | 只包装 `World.prototype.processLoadingQueue`，`world.js` 文件内容零改动 |
