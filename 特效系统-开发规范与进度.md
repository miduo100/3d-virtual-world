# 特效/动效系统 —— 开发规范与进度表

> 本文档是特效系统多会话并行开发的**唯一权威依据**。
> 每个开发会话（无论谁开、无论并行几个）开场必须先读本文档，复述进度，再领任务。
> 任何跨会话依赖的接口变更：**先改本文档并登记"变更记录"，再改代码**。

- 版本：v1.0（2026-09-11 定稿）
- 状态：规划完成，未开始开发
- 产品决策记录：①内置声明式 + 脚本导入双类型同步做；②完整触发系统（接近/传送门/事件/定时/常驻）；③允许引入 GSAP 本地 vendor（约 70KB，懒加载）；④特效与世界编辑器"轻集成"（编辑器内一律代理标记 + 门控预览，绝不实时全量播放）；⑤资产/绑定两层分离（资产管外观，绑定管位置/触发/定时）。

---

## 第一节 红线约束（所有会话必须遵守）

### 1.1 文件红线
1. 所有新建文件 ≤500 行（理想状态），绝对不超过 1000 行。
2. 禁止向黑名单大文件追加任何功能代码：`world.js`(299KB)、`admin.js`、`federation.js`、`federationSystem.js`、`geometryBuilder.js`、`aiSceneGenerator.js`、`templates.js` 等。
3. 本工程对存量文件的改动上限（超出一律禁止）：
   - `public/js/world.js`：仅允许 2 处钩子 —— ① animate 循环内 1 行 `window.EffectRuntime && EffectRuntime.update(dt)`；② 传送门使用处理处 1 行 `window.dispatchEvent(new CustomEvent('game:portal-use', { detail: {...} }))`。
   - `public/index.html`：仅允许追加新脚本引入（带版本号）。
   - `admin.html`：仅允许追加「特效动效」一级菜单与容器挂载点。
   - `src/server.js`：仅允许 2 行路由挂载。
4. 单文件并行修改禁止：同一会话内对同一文件的多次修改必须串行，且改完后重新搜索验证每个目标（防写锁/快照竞争导致改动丢失）。

### 1.2 性能红线
1. 特效灯光一律通过 `ctx.acquireLight()` 走既有 `lightPool`（12 盏常驻池），特效代码中出现 `new THREE.PointLight` 一律导入失败/拦截。
2. 新创建的 object3d 必须进 `compileBudget` 预热后 reveal，禁止渲染侧同步创建着色器程序。
3. 特效材质统一 `depthWrite:false`，默认 additive 混合；透明特效同屏数量独立计数。
4. 预算默认值（全部写成 system_config 可调项）：
   - 总粒子数 ≤ 20000
   - 特效灯光 ≤ 4（从 lightPool 划特效专用配额）
   - 特效 draw call ≤ 80
   - 同屏激活特效实例 ≤ 12
   - 特效 update 帧预算 ≤ 2ms/帧（超时 round-robin 轮转跳过）
5. 距离调度：激活半径 80m / 卸载半径 150m（滞回防抖）。
6. 触发判定与距离判定全部寄生在调度器既有 0.5s 周期扫描上，**禁止新增任何每帧循环**。
7. 定时播放未到点时零运行成本（仅 0.5s 扫描里一次时间比较）；到点创建、播完即毁、逐项 dispose。
8. 触发型特效预算抢占窗口 3 秒（保证关键时刻特效必出，抢占期间挤掉最低优先级常驻实例）。

### 1.3 安全红线
1. 沙箱执行环境禁裸访问 `document / window / localStorage / fetch`，统一经 `ctx` 注入 API。
2. 一切用户输入走参数化 SQL（禁止字符串拼接）；后台写接口必须挂 `authenticateAdminToken`；管理操作写 `logAdminAction`。
3. 前端展示用户可控文本一律 `textContent`，禁止 `innerHTML`。
4. 导入代码必须经 `src/services/fxImport.js` 静态规范化（NaN 防护、DOM 剥离、成本申报、灯光改写），`import_status: ok` 才允许被绑定使用。

### 1.4 契约冻结
跨会话依赖的接口以本文档第三、四节为准。任何修改必须：先改文档 → 变更记录登记 → 再改代码 → 通知其他并行会话（由用户转达或进度表标注）。并行会话不得擅自变更对外接口。

### 1.5 会话协议
- 开场：读本文档进度表（第八节），复述当前状态，再开始本会话任务。
- 收尾三件事：① 更新进度表 ② git 提交 ③ 里程碑写记忆。**不留半成品**——做不完的部分在进度表标"待续"并写明断点。
- git 提交中文消息必须用 UTF-8 消息文件 + `git commit -F 文件`（PowerShell 下 `-m "中文"` 会 GBK 乱码）。

### 1.6 测试纪律
1. 每个里程碑必须交付可重跑验收脚本 `scripts/accept_fx_*.js`。
2. playwright 注意事项：`page.evaluate` 只接受 1 个参数（多参数包对象）；有头模式用 `channel:'chrome'`；console error 断言过滤浏览器扩展噪音（runtime.lastError / index.global.js / favicon 404）。
3. 改动任何被 index.html 引用的前端文件必须递增其引用版本号（`?v=N`），否则浏览器缓存导致"修了没效"。
4. 测试账号 `diag_tmp_1 / Diag#2026tmp`；登录限流（IP 1 小时锁）会挡测试脚本，重启本地服务器清除内存计数器。
5. 服务器端口 3002。

### 1.7 i18n 纪律
1. 中英两语言 JSON key 必须完全一致（改完 node 脚本校验 key 数量相等）。
2. 零功能改动原则：只加 data-i18n/data-i18n-placeholder 属性与文案，不动 id/class/onclick/逻辑。
3. `[data-i18n]` 经 textContent 应用会清空子元素，含子元素的提示框须拆成纯文本子元素。
4. 动态渲染内容在 `reloadCurrentPageContent()` 补对应分支；脚本加载早于 i18n 初始化的文案用 getter 形式延迟取翻译。

### 1.8 部署同步
每会话把新增/修改文件清单记入第九节"部署同步清单"，最终收尾统一同步 ubuntu-deploy-package。

---

## 第二节 数据结构（契约）

### 2.1 表 `fx_assets`（特效/动效资产库）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | SERIAL PK | |
| name | VARCHAR NOT NULL | 资产名 |
| description | TEXT | 描述 |
| tags | TEXT[] | 标签 |
| fx_type | VARCHAR NOT NULL | `particle` / `script` / `gsap` / `video` / `model_anim` |
| source | JSONB NOT NULL | `{kind:'builtin', def:{参数schema+默认值}}` 或 `{kind:'code', code:'...'}` 或 `{kind:'url', url:'...'}` |
| thumbnail_url | VARCHAR | 缩略图（保存/试播时自动截帧生成） |
| duration_ms | INT DEFAULT 0 | 资产原生时长（0=循环型），时间轴排布依据 |
| cost_hint | JSONB | 成本申报 `{particles, lights, draw_calls, fill_rate, update_ms}` |
| min_quality | VARCHAR DEFAULT 'low' | 最低画质档（low/mid/high） |
| source_type | VARCHAR DEFAULT 'paste' | 导入来源 paste/import/url |
| import_status | VARCHAR DEFAULT 'ok' | ok / warning / failed |
| auto_fixes | JSONB | 导入自动修复清单 |
| created_by / created_at / updated_at | | 审计 |

### 2.2 表 `fx_bindings`（绑定与排期实例）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | SERIAL PK | |
| world_id | BIGINT NOT NULL | 按 world_id 隔离（联邦架构） |
| asset_id | INT NOT NULL REFERENCES fx_assets(id) | |
| target | JSONB NOT NULL | 绑定目标，见 2.3 |
| params | JSONB DEFAULT '{}' | 播放参数覆盖，见 2.4 |
| trigger | JSONB DEFAULT '{"type":"always"}' | 触发配置，见 2.5 |
| timeline | JSONB | `{slot_ms:0, group:'开场秀'}` 时间轴排期 |
| enabled | BOOLEAN DEFAULT true | |
| priority | INT DEFAULT 5 | 预算仲裁优先级（越小越优先） |
| created_by / created_at / updated_at | | 审计 |

### 2.3 target 四形态（契约）

```json
{ "type": "world",   "pos": [x,y,z], "rot": [0,0,0], "scale": 1 }
{ "type": "object",  "object_id": 123, "offset": [dx,dy,dz] }
{ "type": "character", "character_id": "...", "bone": "mixamorigSpine", "offset": [dx,dy,dz] }
{ "type": "model_path", "path": "/uploads/...", "offset": [dx,dy,dz] }
```

### 2.4 params 覆盖项（契约）

```json
{ "duration_ms": 8000, "size": 1.0, "speed": 1.0, "intensity": 1.0,
  "loop": false, "url": "覆盖资产source.url", "color": "#ff0000",
  "use_event_position": false }
```
未填的项回退资产默认值。

### 2.5 trigger 五形态（契约）

```json
{ "type": "always" }
{ "type": "radius",  "r": 5, "cooldown": 10, "retrigger": "once" }   // once|reset|resume
{ "type": "portal",  "portal_id": 453, "on": "enter" }               // enter|leave
{ "type": "event",   "name": "player:teleport-in" }
{ "type": "schedule", "mode": "daily",   "at": "20:00", "days": [0,1,2,3,4,5,6] }
{ "type": "schedule", "mode": "once",    "at": "2026-09-11 20:00:00" }
{ "type": "schedule", "mode": "interval","every_sec": 300, "duration_ms": 8000 }
```
- radius/portal/event 与 schedule 互斥可配（同一绑定只选一种主触发）。
- 事件触发 payload 可携带 `position`，绑定 `params.use_event_position:true` 时特效在 payload 位置播放。

---

## 第三节 接口契约（后端）

### 3.1 资产 API（`src/routes/fxAssets.js`，挂载 `/api/fx-assets`）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/` | 公开 | 列表（search/tag/type/limit/offset，不返回 code 全文） |
| GET | `/:id` | 公开 | 单个（含完整 source） |
| POST | `/` | admin | 新建 |
| PUT | `/:id` | admin | 编辑 |
| DELETE | `/:id` | admin | 删除（有绑定时拒绝或级联提示） |
| POST | `/import` | admin | 导入规范化（调 fxImport 服务，返回检查报告） |
| POST | `/:id/validate` | admin | 试运行校验（成本申报核对） |

### 3.2 绑定 API（`src/routes/fxBindings.js`，挂载 `/api/fx-bindings`）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/nearby?x=&z=&r=` | 公开 | 按距离分发激活集（含资产 source 摘要+参数+触发），**玩家端唯一高频接口** |
| GET | `?world_id=` | admin | 全量列表（管理台用） |
| POST | `/` | admin | 新建（保存时做预算合计校验，超预算返回 warning 不阻断） |
| PUT | `/:id` | admin | 编辑 |
| DELETE | `/:id` | admin | 删除 |

### 3.3 全局配置（system_config）

`fx_enabled`（总开关）、`fx_budget_particles`(20000)、`fx_budget_lights`(4)、`fx_budget_drawcalls`(80)、`fx_budget_active`(12)、`fx_quality_mode`(auto)。走既有 configService 存取。

---

## 第四节 接口契约（前端）

### 4.1 文件清单与职责（全部新建）

| 文件 | 职责 | 目标行数 |
|---|---|---|
| `database/fx_effects_init.sql` | 双表 DDL | ~80 |
| `src/routes/fxAssets.js` | 资产 CRUD+导入端点 | ~300 |
| `src/routes/fxBindings.js` | 绑定 CRUD+nearby 分发+预算校验 | ~380 |
| `src/services/fxImport.js` | 导入规范化/安全检查 | ~400 |
| `public/effect_editor.html` | 资产编辑/预览独立页 | ~480 |
| `public/js/adminFxAssets.js` | 后台资产库页签 | ~450 |
| `public/js/adminFxBindings.js` | 后台绑定排期页签 | ~480 |
| `public/js/fxRuntime.js` | 运行时入口+事件总线+update 钩子 | ~350 |
| `public/js/fxScheduler.js` | 距离调度/LOD/视锥/0.5s 扫描 | ~320 |
| `public/js/fxTriggers.js` | 五类触发语义+定时+抢占窗口 | ~250 |
| `public/js/fxBudget.js` | 预算仲裁/画质分档/降级 | ~300 |
| `public/js/fxSandbox.js` | 脚本特效沙箱 | ~260 |
| `public/js/fxBuiltins.js` | 内置声明式特效族 | ~460 |
| `public/js/fxGsapLoader.js` | GSAP 懒加载与 ticker 治理 | ~80 |
| `js/lib/gsap.min.js` | GSAP vendor（懒加载，不直接引入 index.html） | vendor |

### 4.2 运行时对外接口（契约，冻结）

```javascript
// fxRuntime 暴露（window.EffectRuntime）
EffectRuntime.update(dt)              // world.js animate 内唯一钩子，1 行
EffectRuntime.emit(name, payload)     // 事件总线：其他系统触发特效入口
EffectRuntime.on(name, handler)       // 订阅（内部模块用）
EffectRuntime.refreshBindings()       // nearby 增量刷新（管理端改配置后调用）
EffectRuntime._diag()                 // 诊断：激活数/预算占用/异常列表

// 特效实现契约（内置与脚本统一）
function createEffect(ctx) {
  // ctx: { THREE, container(特效私有Group), position, params, size,
  //        acquireLight(), gsap(懒加载代理，未加载时访问会触发加载), time }
  return {
    object3d,                    // 必须挂到 ctx.container
    update(dt, elapsed) {},      // 受预算调度，可能被跳帧
    dispose() {}                 // 必须：geometry/material/texture/timer 逐项清理
  };
}
```

### 4.3 index.html 引入顺序（契约）

```
lightPool.js → compileBudget(既有) → fxBudget.js → fxScheduler.js → fxTriggers.js
→ fxSandbox.js → fxBuiltins.js → fxGsapLoader.js → fxRuntime.js?v=1
（全部位于 world.js 之后、玩家入口脚本之前；各自带 ?v= 版本号）
```

### 4.4 事件契约

| 事件名 | 来源 | payload |
|---|---|---|
| `game:portal-use` | world.js 钩子（传送门使用） | `{portalId, position, characterId}` |
| `player:teleport-in` | WebSocket 玩家加入/传送到达消息转发 | `{position, characterId, characterName}` |
| 自定义任意 | 其他系统 `EffectRuntime.emit` | 自定义（可含 position） |

---

## 第五节 交互与界面规划（定稿摘要）

1. **后台三个页签**（admin「特效动效」菜单下）：① 资产库（卡片列表+徽标+成本标注）② 绑定与排期（每绑定一卡片：资产下拉/四种位置绑定/五种触发联动表单/预算合计条/单条启停）③ 时间轴预演（轨道时间轴、播放头扫过即播、只加载涉及资产+简化地面、成本仪表超限变灰）。
2. **世界编辑器**：左侧「特效」面板（资产小卡拖入场景→代理标记；标记可拖动/缩放/双击弹精简配置浮窗；选中可"预览播放"，同屏上限 3 个，走 compileBudget 预热）。编辑器内特效一律代理化，绝不实时创建粒子系统。
3. **玩家端零界面**：全自动按调度/触发/定时播放。
4. 性能结论基线：单个轻特效 ≈1 draw call + <0.5ms/帧；预算内 12 同屏对 60fps 影响 <5%；additive 填充率是首要 GPU 成本（1 个 800 粒子烟花 ≈2.3 倍全屏 overdraw）。

---

## 第六节 里程碑与会话拆分（含各会话触发提示词）

> 会话 0 为串行起点；会话 2/3/4/5 相互独立可并行（各自新文件，无共享文件写入），依赖 M1/M2 的契约冻结。

### 会话 0：规范落盘 + 数据库与后端（M1）
范围：本文档创建；`fx_effects_init.sql`；`fxAssets.js`/`fxBindings.js`；server.js 挂载；curl 冒烟。
提示词：
```text
继续特效系统开发。请先读 l:\shegnjir185\特效系统-开发规范与进度.md 的规范红线、
接口契约和进度表，复述当前进度后开始本会话任务（M1 基础设施）：
1. 创建 database/fx_effects_init.sql：fx_assets / fx_bindings 双表（字段以规范文档第二节为准）；
2. 创建 src/routes/fxAssets.js 与 src/routes/fxBindings.js（端点以第三节为准，含
   GET /nearby 距离分发、定时/触发字段校验、预算合计校验）；
3. src/server.js 仅 2 行路由挂载；
4. 全部新文件 ≤500 行，参数化 SQL，管理端点挂 authenticateAdminToken。
完成后：curl 冒烟验证所有端点 → 更新进度表 → git 提交（UTF-8 消息文件 + commit -F）。
```

### 会话 1：前端运行时 + 内置粒子特效（M2）
提示词：
```text
继续特效系统开发。先读 l:\shegnjir185\特效系统-开发规范与进度.md，复述进度后开始
本会话任务（M2 运行时）：
1. fxRuntime.js / fxScheduler.js / fxBudget.js / fxBuiltins.js（对外接口与预算默认值
   以第四、1.2 节契约为准，内置特效输出统一 createEffect(ctx) 契约对象）；
2. index.html 按第四节 4.3 顺序引入（带 ?v=1）；
3. world.js 只允许加 2 处钩子（animate 1 行 update + 传送门 1 行事件派发），
   除此之外一行都不许加。
验收：scripts/accept_fx_runtime.js——12 实例同屏 60fps 无长帧、距离进出正确
创建/休眠、dispose 无泄漏、0 console error。完成后更新进度表 + git 提交。
```

### 会话 2：沙箱 + 导入器 + GSAP + 资产编辑页（M3）
提示词：
```text
继续特效系统开发。先读规范文档，复述进度后开始（M3 沙箱导入）。依赖 M1/M2 完成，
不得变更 fxRuntime/fxScheduler 已冻结的对外接口。
范围：fxSandbox.js（契约执行/单实例熔断/强制 dispose）；src/services/fxImport.js
（禁 document/window/localStorage/裸 PointLight，自动改写 ctx.acquireLight()，
NaN 防护，成本申报）；fxGsapLoader.js（gsap.min.js 本地 vendor 懒加载）；
public/effect_editor.html（表单模式+代码模式+预览画布+试播+自动截帧缩略图）。
验收：scripts/accept_fx_sandbox.js——恶意代码被拦截、异常熔断不影响渲染主循环、
dispose 后显存归还、GSAP 无实例时 0 加载。完成后更新进度表 + git 提交。
```

### 会话 3：触发系统全量 + 定时播放（M4）
提示词：
```text
继续特效系统开发。先读规范文档，复述进度后开始（M4 触发与定时）。
范围：fxTriggers.js——五类触发语义（2.5 节契约）+服务器时间基准校正+触发型 3 秒
预算抢占窗口；接入 game:portal-use 事件与 WebSocket player:teleport-in → 事件总线；
触发/距离判定全部寄生 fxScheduler 0.5s 扫描，禁止新增每帧循环。
验收：scripts/accept_fx_triggers.js——接近/传送门/事件/定时四类端到端触发，
冷却与 retrigger 语义正确，未到点定时绑定零运行成本。完成后更新进度表 + git 提交。
```

### 会话 4：后台管理台（M5）
提示词：
```text
继续特效系统开发。先读规范文档，复述进度后开始（M5 后台管理台）。
范围：admin.html 仅加「特效动效」菜单与容器挂载点；adminFxAssets.js（资产库页签）；
adminFxBindings.js（绑定排期页签：绑定卡片/资产下拉/四种位置绑定/五种触发联动表单/
预算合计条/单条启停）。i18n 两语言 key 完全对齐，遵守 1.7 节纪律。
验收：建资产→绑世界→配触发→玩家端生效全流程，0 console error。
完成后更新进度表 + git 提交。
```

### 会话 5：时间轴预演 + 编辑器拖拽面板（M6）
提示词：
```text
继续特效系统开发。先读规范文档，复述进度后开始（M6 预演与编辑器集成）。
范围：后台「时间轴预演」页签（轨道时间轴/slot_ms 拖拽/播放头扫过即播/只加载涉及
资产+简化地面/成本仪表超限变灰）；world_editor「特效」侧栏面板+场景代理标记
（Sprite 图标+包围盒，拖动/缩放/双击精简配置浮窗/预览播放上限 3 个走 compileBudget
预热）。编辑器内特效一律代理化，绝不实时创建粒子系统。
验收：拖拽摆位→双击配置→预览播放→保存后玩家端生效全链路；1000 个代理标记无卡顿；
0 console error。完成后更新进度表 + git 提交，并登记第九节部署同步清单。
```

### 会话 6：综合验收与收尾（M7，最后串行）
范围：全链路压测（低配档降级/预算挤占/GSAP 加载时序）、全部 accept 脚本回归、ubuntu-deploy-package 同步、文档归档。

---

## 第七节 验收基线（M7 总验收判据）

1. 12 特效实例同屏 60fps（真实 GPU），0 长帧（>150ms）；预算超限时按优先级休眠且 console 静默。
2. 五类触发全部端到端可用，冷却/retrigger/服务器时间基准正确。
3. 沙箱：恶意代码 0 执行、单实例异常 0 扩散、dispose 后 renderer.info.memory 归还。
4. 编辑器：1000 代理标记流畅、预览上限 3 生效、编译预热无冻结帧。
5. 定时未到点绑定零运行成本（_diag 可证）。
6. 全程 0 console error（过滤扩展噪音）。

---

## 第八节 进度表（唯一权威进度记录）

| 里程碑 | 会话 | 状态 | 交付物 | 完成时间 |
|---|---|---|---|---|
| M0 规划与本文档 | 本会话 | ✅ 完成 | 本文档 | 2026-09-11 |
| M1 双表+后端路由 | 会话 0 | ⬜ 未开始 | fx_effects_init.sql / fxAssets.js / fxBindings.js | |
| M2 运行时+内置特效 | 会话 1 | ⬜ 未开始 | fxRuntime/Scheduler/Budget/Builtins + 验收脚本 | |
| M3 沙箱+导入+GSAP+编辑页 | 会话 2 | ⬜ 未开始 | fxSandbox / fxImport / fxGsapLoader / effect_editor.html | |
| M4 触发+定时 | 会话 3 | ⬜ 未开始 | fxTriggers + 验收脚本 | |
| M5 后台管理台 | 会话 4 | ⬜ 未开始 | adminFxAssets / adminFxBindings + i18n | |
| M6 时间轴+编辑器集成 | 会话 5 | ⬜ 未开始 | 时间轴预演页 / world_editor 特效面板 | |
| M7 综合验收收尾 | 会话 6 | ⬜ 未开始 | 全量回归 + 部署同步 | |

**当前断点**：M1 未开始。用户指示"先写规则，不要开发"——本文档落盘后等待用户指令再启动 M1。

## 变更记录

| 日期 | 变更 | 登记会话 |
|---|---|---|
| 2026-09-11 | v1.0 初版定稿 | 规划会话 |

---

## 第九节 部署同步清单（持续登记，收尾统一同步 ubuntu-deploy-package）

| 文件 | 来源里程碑 | 状态 |
|---|---|---|
| （暂无——M1 未开始） | | |
