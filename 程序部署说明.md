# virtual-world 部署说明

## 环境要求
- Node.js：推荐版本 20.x，最低版本 18.x
- 包管理器：NPM
- 数据库：PostgreSQL 18.1，最低版本 PostgreSQL 17
- 端口：3002

## 快速部署

### 1. 安装依赖

> **提示**：项目内已包含 `node_modules` 目录，可直接使用，无需重复安装。

如需重新安装依赖：
```bash
npm install
```

**Linux 用户**：项目根目录下的 `linux_node_modules.gz` 是 Linux 环境的预编译依赖包，解压后即可使用：
```bash
tar -xzf linux_node_modules.gz
```

### 2. 配置环境变量

> **请先完成数据库导入**：参照 `数据库导入.md` 文件中的步骤，先创建数据库并导入数据，再进行下面的 `.env` 配置。

项目根目录下已包含 `.env` 文件，直接用文本编辑器（如记事本、VS Code）打开编辑即可，无需在命令行中手动操作。

必须修改以下配置项：
- `DB_PASSWORD` — 数据库密码（填写你本地 PostgreSQL 的密码）
- `DB_NAME` — 数据库名称（默认 `virtual_world`）
- `DB_USER` — 数据库用户名（默认 `postgres`）
- `DB_HOST` — 数据库地址（本地为 `localhost`）
- `DB_PORT` — 数据库端口（默认 `5432`）
- `JWT_SECRET` — JWT 密钥（可用 `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` 生成）
- `ADMIN_JWT_SECRET` — 管理员 JWT 密钥（同上方式生成）
- `WORLD_NAME` — 世界名称
- `WORLD_URL` — 世界访问地址

### 3. 数据库导入

数据库已通过 `db_export.sql` 完整导出，直接导入即可使用。具体步骤请参照 `数据库导入.md`。

### 4. 启动服务
```bash
# 开发模式
npm start

# 生产模式（推荐 PM2）
npm install -g pm2
pm2 start src/server.js --name virtual-world
pm2 save
pm2 startup
```

### 5. 访问

**直接访问**：
- 用户端: http://你的服务器IP:3002/
- 管理后台: http://你的服务器IP:3002/admin.html
- 默认管理员: admin / admin123456（请立即修改）

**通过 Nginx 反向代理绑定域名访问**：
- 将域名解析到服务器 IP
- 配置 Nginx 反向代理，将 80/443 端口请求转发到本地 3002 端口
- 访问 `https://你的域名` 即可，无需带端口号

## AI Agent 接入：部署要点与排查

> **为什么单独一节**：AI Agent 接入（`/.well-known/virtual-world-agent.json`）是外部 AI
> 「发现并进入」本世界的**唯一入口**。这条链路上任何一环出错，AI 都会在第一步就失败，
> 而且**大多不报错、只是静默不生效** —— 所以部署后请按下面的清单逐项验证。

### 1. Nginx 反向代理：两个必须设置的请求头

在代理到 3002 的 `location` 里必须有这两行：

```nginx
proxy_set_header X-Forwarded-Proto $scheme;                      # ① 告诉后端"外部是 https"
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;   # ② 传真实客户端 IP
```

**宝塔面板注意**：反向代理配置**不在**站点主配置里，而在
`/www/server/panel/vhost/nginx/proxy/<域名>/<hash>_<域名>.conf`（由主配置 `include` 引入）。
定位命令：

```bash
grep -rn "3002" /www/server/panel/vhost/nginx/
```

#### ① 少了 `X-Forwarded-Proto` → AI 客户端第一跳就被拦

后端 `deriveBaseUrl()`（`src/routes/agent/meta.js`）拿不到原始协议时只能猜 `http://`，
于是发现文档会广播：

```json
"world":     { "url": "https://你的域名/" },                 ← 对
"endpoints": { "apiBase": "http://你的域名/api/agent/v1" }  ← 错：明文 http
```

同一份响应里两个来源协议不一致。**https 页面里的 AI 客户端第一跳就会被浏览器的
Mixed Content 拦掉**，表现是"AI 死活进不来"，但服务器日志里什么都看不到。

> 代码侧有"按权威 `WORLD_URL` 兜底校正协议"的纵深防御，**但正确做法仍是把这一行加上**。

验证：

```bash
curl -s "https://你的域名/.well-known/virtual-world-agent.json?t=$RANDOM" | grep -o '"apiBase": "[^"]*"'
# 期望输出 https://...    若为 http://... 就是漏了这一行（或没 reload）
```

#### ② 少了 `X-Forwarded-For` → 全世界被算成同一个 IP（更隐蔽）

`resolveClientIp()`（`src/middleware/clientIp.js`）拿不到真实 IP，反代后**所有访客共享
同一个 IP**。而 AI Agent 接入有两条按 IP 的限制：**每 IP 1 个游客连接** + **每 IP 10 张票/小时**
→ 结果是**全世界同时只能有一个游客 AI 在线**，第二个进来直接 `GUEST_IP_CONCURRENCY` 被拒。

这个坑**不会报错**，只会让"AI 接入看起来很冷清"。多世界部署、手工配的站点必须检查。

验证：

```bash
grep -c "X-Forwarded-For" /www/server/panel/vhost/nginx/proxy/<域名>/*.conf   # 期望 ≥ 1
```

### 2. 宝塔环境的三个坑

| 坑 | 说明与处置 |
|---|---|
| `#PROXY-START` / `#PROXY-END` 之间是**面板托管**的 | 手工加的行会在下次"反向代理 → 保存"时被面板模板覆盖。改完**不要再点面板保存** |
| 站点配置里那条 `location ~ \.well-known` | **不需要为它改动**。反代里的 `location ^~ /` 带 `^~`，匹配后即停止正则匹配，所以它不生效；且该规则的黑名单（`php\|jsp\|py\|js\|css…`）里**没有 json** |
| 终端里 `curl https://自己的域名` 无输出 | 不是配置错，是**阿里云 ECS 的 hairpin NAT 限制**。请用 `curl -H "Host: 你的域名" http://127.0.0.1:3002/...`，或从**外部**验证 |

改完 Nginx 必须：`nginx -t && nginx -s reload`

### 3. 首页 SEO 必须「服务端注入」，不要只用 JS

**坑**：如果首页的 `<title>` / `<meta>` 靠浏览器端 JS（`fetch('/api/config/seo')` 后改写
`document.title`）更新，那么 —— **AI 爬虫与搜索引擎都不执行 JavaScript**，它们永远只看
HTML 文件里硬编码的那份文案，**后台「SEO TDK 配置」对它们完全无效**。

最阴的地方：当**后台配置值与 HTML 硬编码值恰好相同时，你完全看不出问题**，
只有在后台改了值之后才会发现"改了没用"。

**本项目的做法（已实现）**：`src/services/seoHtmlInjector.js` 在**服务端**把
`system_config` 里的 SEO 配置注入首页 HTML —— 浏览器与爬虫拿到同一份：

```
后台「🔍 SEO TDK 配置」（唯一权威数据源）
        ↓ 服务端注入（10 秒缓存）
    同一份 HTML ──┬─> 浏览器 ✅
                 └─> AI 爬虫 / 搜索引擎 / 分享卡片 ✅
```

改动代码时**请勿破坏的三点**：

1. `src/server.js` 里 `app.get(['/', '/index.html'], seoHtmlInjector.handler)` 
   **必须注册在 `express.static` 之前**，否则请求走静态文件、注入不生效
2. 注入失败会 `next()` 回退静态文件 → **首页永不 500**（不要改成抛错）
3. 改完 `server.js` **必须重启 Node**（`pm2 restart <进程名>`），否则注入路由根本不存在

**如何判断注入到底生效没有**（只看首页会误判 —— 静态兜底值可能恰好也是新文案）：

| 现象 | 含义 |
|---|---|
| 响应头 `Content-Type: text/html; charset=utf-8`（**小写**） | ✅ 注入生效 |
| 响应头 `Content-Type: text/html; charset=UTF-8`（**大写**） | ❌ 走的是 `express.static`，注入没生效 |

更可靠的方法 —— 对比两个数据源是否逐字一致：

```bash
curl -s "https://你的域名/api/config/seo"                                  # 期望值
curl -s "https://你的域名/?t=$RANDOM" | grep -o "<title>[^<]*</title>"      # 实际值
# 两者应完全一致。不一致 = 注入未生效（多为 server.js 未部署或 Node 未重启）
```

### 4. 部署后 AI 接入自检清单（5 条，逐项跑）

```bash
D="你的域名"
curl -s "https://$D/.well-known/virtual-world-agent.json?t=$RANDOM" | grep -o '"apiBase": "[^"]*"'  # ① 必须 https://
curl -s -o /dev/null -w "agents=%{http_code}\n"  "https://$D/agents/"       # ② 期望 200
curl -s -o /dev/null -w "llms=%{http_code}\n"    "https://$D/llms.txt"      # ③ 期望 200
curl -s -o /dev/null -w "robots=%{http_code}\n"  "https://$D/robots.txt"    # ④ 期望 200
curl -s -o /dev/null -w "sitemap=%{http_code}\n" "https://$D/sitemap.xml"   # ⑤ 期望 200
```

全部通过后，任何 AI 只要知道域名，就能完成：**发现世界 → 零凭证领取身份 → 进入世界行走说话**。

再验一步「AI 是否真的能进」（可选，会消耗 1 张游客票）：

```bash
cd <项目目录> && AGENT_HOST=https://$D node examples/agent-client/node-agent.mjs
```

### 5. Agent 总开关（默认关闭）

| 配置 | 默认 | 说明 |
|---|---|---|
| `agent_enabled` | **false** | **总开关**。部署后若要对外开放 Agent 接入，需在后台「🤖 AI Agent」页改为 `true` |
| `max_agents` | 50 | 同时在线 Agent 上限 |
| `agent_push_default` | eco | 默认推送档（eco / standard / realtime） |

> `agent_enabled=false` 时：**发现文档、`/capabilities`、`/openapi.json` 仍然可读**
> （刻意设计：连"开关是否打开"都要先能查到），但 `POST /api/agent/v1/session` 与
> `/guest/session` 会返回 `503 AGENT_DISABLED_GLOBALLY`。

### 6. 故障速查表

| 现象 | 最可能原因 | 处置 |
|---|---|---|
| 发现文档 `apiBase` 是 `http://` | Nginx 缺 `X-Forwarded-Proto` | 补头 + `nginx -s reload` |
| 第二个 AI 进来被拒（`GUEST_IP_CONCURRENCY`） | Nginx 缺 `X-Forwarded-For`，或同 IP 重复签票 | 补头；确认每 IP 限制（10 张票/小时） |
| 后台改了 SEO，爬虫/搜索看不到 | 注入未生效（`server.js` 未部署 / Node 未重启） | 对比 `Content-Type` 大小写 + 首页 title vs `/api/config/seo` |
| `/agents/`、`llms.txt` 返回 404 | 门面文件未部署 | 传 `public/robots.txt`、`sitemap.xml`、`llms.txt`、`agents/index.html` |
| AI 连上但看不到任何描述 | 世界物体的 AI 描述未填 | 后台「上传模型库」补 🤖AI 描述 + 跑 `scripts/sync_agent_descriptions.js` |
| WS 连不上（`/ws/agent`） | Nginx 未透传 `Upgrade` / `Connection` 头 | 见 `README.md` 的 Nginx 配置段 |

## 联邦系统配置

### 中央世界（作为中心让其他世界连过来）
```env
WORLD_URL=https://你的公网域名        # 必须公网可访问
AUTO_CONNECT_CENTRAL=false            # 中心世界自身不参与自连
```

### 子世界（默认行为：启动即自动加入联邦）
```env
WORLD_URL=https://你的公网域名        # 必须公网可访问，中心世界会回拨校验
CENTRAL_WORLD_URL=https://miduo100.com  # 不配置时默认就是这个地址
AUTO_CONNECT_CENTRAL=true             # 设为 false 即退出联邦
```

> - `localhost` / `192.168.x` / `10.x` 地址会被自动跳过（仅本地测试可用 `FEDERATION_ALLOW_PRIVATE=1` 强制放行）。
> - 启动日志会明确打印结果：`✅ 成功连接到中心世界！` / `ℹ️  自动连接已禁用，跳过` / `⚠️  [联邦] 本世界 worldUrl 为内网/本机地址…`。
> - 是否已连上，可查 `GET /api/federation/central-status`，或在中心世界的「受信任世界」列表里看到本世界。

## Docker 部署
```bash
docker compose up -d --build
```

## 目录结构
```
├── src/              # 后端源码
│   ├── server.js     # 主入口
│   ├── routes/       # API路由
│   ├── services/     # 业务服务
│   ├── middleware/   # 中间件
│   ├── database/     # 数据库模块
│   ├── websocket/    # WebSocket
│   └── utils/        # 工具
├── public/           # 前端资源
│   ├── js/           # 前端JS
│   ├── models/       # 3D模型
│   ├── uploads/      # 上传文件
│   └── *.html        # 页面
├── database/         # 数据库SQL（init.sql + migrations）
├── uploads/          # 根级上传目录（角色模板/建筑图片）
├── .env.example      # 环境变量模板
├── package.json      # 依赖配置
└── Dockerfile        # Docker配置
```

## 技术支持
济宁米多信息科技有限公司
联系方式: 888@miduo100.com / 15660440944
