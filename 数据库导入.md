> **新建PostgreSQL数据库（数据库名用纯小写）**

# 虚拟世界项目 - 数据库导入部署说明书

## 一、适用场景

将完整的数据库 SQL 文件（`db_export.sql`）导入到生产服务器（Linux/Ubuntu，宝塔面板），并完成项目部署配置的过程。

本说明书总结了实际部署中遇到的所有典型问题及解决方案。

---

## 二、整体流程概览

```
上传 SQL 文件到服务器
        ↓
在服务器创建目标数据库
        ↓
导入 SQL 数据
        ↓
权限检查（关键步骤！）
        ↓
如有权限问题 → 给应用账号授权
        ↓
修改 .env 配置文件
        ↓
重启 Node 服务
        ↓
验证 API 是否正常
```

---

## 三、详细操作步骤

### 3.1 上传 SQL 文件到服务器

通过 SCP、SFTP 或宝塔面板文件管理器，将 `db_export.sql` 上传到服务器，例如：

```
/www/wwwroot/virtual-world-bt/db_export.sql
```

---

### 3.2 在服务器创建目标数据库

#### 方式 A：宝塔面板创建（推荐）

1. 登录宝塔面板
2. **数据库 → PostgreSQL → 添加数据库**
3. 填写数据库名、用户名、密码（**建议全小写**，详见第六章）
4. 记录下宝塔生成的：
   - 数据库名（如 `s8wiykdryzpjrrp4`）
   - 用户名（如 `S8WiYKdRyzPJrrp4`）
   - 密码（如 `S8WiYKdRyzPJrrp4`）

#### 方式 B：命令行创建

```bash
# 切换到 postgres 用户
sudo -u postgres psql

# 创建数据库和用户（建议全小写）
CREATE USER vw_app WITH PASSWORD '你的强密码';
CREATE DATABASE virtual_world OWNER vw_app;
GRANT ALL PRIVILEGES ON DATABASE virtual_world TO vw_app;
\q
```

---

### 3.3 导入 SQL 数据

```bash
# 用 postgres 超级用户导入
sudo -u postgres psql -d 你的数据库名 -f /www/wwwroot/virtual-world-bt/db_export.sql

# 验证导入结果（检查表数量）
sudo -u postgres psql -d 你的数据库名 -c "SELECT count(*) FROM information_schema.tables WHERE table_schema='public';"
```

**期望结果**：返回 60+ 张表。

#### 常见问题：编码错误 "0x0ff 0xfe 在 UTF8 没有相对应值"

**现象**：
```
psql: error: 编码"GBK"的字符0x0ff 0xfe在编码"UTF8"没有相对应值
```

**原因**：`db_export.sql` 文件被转存或另存成了 **UTF-16 LE** 格式，而不是 UTF-8。UTF-16 LE 文件头部有 BOM 标记 `0xFF 0xFE`，PostgreSQL 客户端（psql）按 UTF-8 解析时会报此错误。

**解决方案**：

```bash
# 方式 1：用 iconv 转换编码（推荐）
iconv -f UTF-16LE -t UTF-8 db_export.sql > db_export_utf8.sql

# 然后导入转换后的文件
sudo -u postgres psql -d 你的数据库名 -f db_export_utf8.sql
```

```bash
# 方式 2：用 PowerShell（Windows 上）
Get-Content db_export.sql -Encoding Unicode | Set-Content db_export_utf8.sql -Encoding UTF8
```

**预防建议**：导出 SQL 时直接指定 UTF-8 编码，避免另存为时意外切换编码格式。

---

### 3.4 权限检查（导入后必做）

导入完成后，**先检查权限是否正常**，再决定是否需要授权。

#### 步骤 1：用应用账号测试查询

```bash
# 用应用账号（不是 postgres）测试能否查询表
PGPASSWORD='你的应用账号密码' psql -h 127.0.0.1 -U "你的应用用户名" -d 你的数据库名 -c "SELECT count(*) FROM system_config;"
```

#### 步骤 2：检查结果并处理

根据返回结果，对照下表处理：

| 返回结果 | 说明 | 处理方式 |
|---------|------|---------|
| 返回行数（如 `39`） | ✅ 权限正常 | 跳过 3.5 节，直接去测试序列权限 |
| `permission denied for table system_config` | ❌ 表权限缺失 | 执行 3.5 节授权 |
| `role "xxx" does not exist` | ❌ 用户名大小写问题 | 看第四章问题 4 |
| `password authentication failed` | ❌ 密码不对 | 核对 .env 里的密码 |
| `connection refused` | ❌ 数据库服务没启动 | 看第四章问题 1 |

#### 步骤 3：如果表权限正常，再测序列权限

```bash
# 测试序列权限（INSERT 时需要）
PGPASSWORD='你的应用账号密码' psql -h 127.0.0.1 -U "你的应用用户名" -d 你的数据库名 -c "SELECT nextval('ui_controls_id_seq');"
```

| 返回结果 | 说明 | 处理方式 |
|---------|------|---------|
| 返回数字（如 `2542`） | ✅ 序列权限正常 | 全部权限 OK，去 3.6 修改 .env |
| `permission denied for sequence ui_controls_id_seq` | ❌ 序列权限缺失 | 执行 3.5 节授权 |

---

### 3.5 给应用账号授权（仅当权限检查失败时执行）

如果 3.4 节检查发现权限不足，执行以下命令。

#### 3.5.1 问题原因

- 用 `postgres` 用户导入数据 → 表的 owner 是 `postgres`
- 应用账号默认**没有任何权限**访问这些表和序列
- 必须显式 `GRANT` 授权

#### 3.5.2 授权命令

```bash
# 1. 授予所有表的权限
sudo -u postgres psql -d 你的数据库名 -c 'GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO "你的应用用户名";'

# 2. 授予所有序列的权限（重要！否则 INSERT 时拿不到自增 ID）
sudo -u postgres psql -d 你的数据库名 -c 'GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO "你的应用用户名";'

# 3. 授予 schema 使用权限
sudo -u postgres psql -d 你的数据库名 -c 'GRANT USAGE ON SCHEMA public TO "你的应用用户名";'

# 4. 让未来新建的表也自动授权（防止以后加表又没权限）
sudo -u postgres psql -d 你的数据库名 -c 'ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON TABLES TO "你的应用用户名";'
sudo -u postgres psql -d 你的数据库名 -c 'ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON SEQUENCES TO "你的应用用户名";'
```

#### 3.5.3 授权后重新验证

```bash
# 再次测试表权限
PGPASSWORD='你的密码' psql -h 127.0.0.1 -U "你的用户名" -d 你的数据库名 -c "SELECT count(*) FROM system_config;"

# 再次测试序列权限
PGPASSWORD='你的密码' psql -h 127.0.0.1 -U "你的用户名" -d 你的数据库名 -c "SELECT nextval('ui_controls_id_seq');"
```

两个都能返回结果，说明权限已完整。

---

### 3.6 修改 .env 配置文件

```bash
# 编辑 .env
nano /www/wwwroot/virtual-world-bt/.env
```

修改以下字段：

```env
# 数据库配置（改成服务器上的实际值）
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=你的数据库名
DB_USER=你的数据库用户名
DB_PASSWORD=你的数据库密码

# 服务器配置
PORT=3002
NODE_ENV=production

# 世界配置
WORLD_URL=https://你的域名
```

**验证配置**：

```bash
grep -E "^DB_|^PORT" /www/wwwroot/virtual-world-bt/.env
```

---

### 3.7 重启 Node 服务

**重要：修改 `.env` 后必须重启 Node，否则配置不生效！**

```bash
# 进入项目目录
cd /www/wwwroot/virtual-world-bt

# 用 PM2 启动
pm2 start src/server.js --name virtual-world

# 如果已经在跑，重启
pm2 restart virtual-world

# 设置开机自启（推荐）
pm2 save
pm2 startup
# 按提示执行它给你的命令
```

---

### 3.8 验证部署成功

```bash
# 1. 检查 Node 服务状态
pm2 status

# 2. 测试 API
curl -s http://localhost:3002/api/health
# 期望：{"status":"ok","timestamp":"..."}

curl -s http://localhost:3002/api/federation/info | head -c 200
# 期望：{"success":true,"world":{...}}

curl -s http://localhost:3002/api/config/seo | head -c 100
# 期望：返回 SEO 配置 JSON

# 3. 检查错误日志（应该为空或只有警告）
pm2 logs virtual-world --lines 20 --nostream --err
```

刷新浏览器访问 `https://你的域名`，应该能正常进入游戏。

---

## 四、常见问题排查

### 问题 1：PostgreSQL 服务未启动

**现象**：
```
psql: error: connection to server on socket "/var/run/postgresql/.s.PGSQL.5432" failed: FATAL: could not open shared memory segment "/PostgreSQL.xxx": No such file or directory
```

**原因**：
- `postgresql.service` 是包装服务，实际跑数据库的是 `postgresql@版本-main.service`
- 系统更新或共享内存配置变化后偶发问题

**解决方案**：

```bash
# 1. 查看实际运行的 PostgreSQL 实例服务
systemctl list-units --type=service | grep postgres

# 2. 重启对应版本的实例服务（比如 18 版本）
systemctl restart postgresql@18-main

# 3. 如果重启失败，调大共享内存
sysctl -w kernel.shmmax=17179869184
sysctl -w kernel.shmall=4194304

# 永久生效
echo "kernel.shmmax=17179869184" >> /etc/sysctl.conf
echo "kernel.shmall=4194304" >> /etc/sysctl.conf
sysctl -p

# 4. 再次重启
systemctl restart postgresql@18-main
```

---

### 问题 2：所有 API 返回 500/503 错误

**现象**：
```
GET /api/federation/info 503 (Service Unavailable)
GET /api/config/seo 500 (Internal Server Error)
GET /api/users/character/xxx 500 (Internal Server Error)
```

**原因**：Node 服务没启动，或数据库连不上。

**排查步骤**：

```bash
# 1. 检查 Node 服务是否在跑
pm2 status

# 2. 如果不在跑，启动它
cd /www/wwwroot/virtual-world-bt
pm2 start src/server.js --name virtual-world

# 3. 看错误日志
pm2 logs virtual-world --lines 50 --nostream --err

# 4. 测试数据库连接
PGPASSWORD='你的密码' psql -h 127.0.0.1 -U "你的用户名" -d 你的数据库名 -c "\dt"
```

---

### 问题 3：权限不足（permission denied）

**现象**：
```
Database query error: error: permission denied for table ui_controls
[UIControls] 初始化默认控件失败: permission denied for table ui_controls
```

或：

```
permission denied for sequence ui_controls_id_seq
```

**原因**：
- 表的 owner 是 `postgres`（用 postgres 用户导入的）
- 应用账号没有访问权限
- 序列权限也没授予（INSERT 时拿不到自增 ID）

**解决方案**：参见 3.5 节，执行完整的 GRANT 授权命令。

---

### 问题 4：Role 不存在（role does not exist）

**现象**：
```
ERROR: role "s8wiykdryzpjrrp4" does not exist
```

**原因**：
- PostgreSQL 的 role 名区分大小写
- SQL 里不加双引号会被自动转成小写
- 实际 role 名是 `S8WiYKdRyzPJrrp4`（混合大小写），但 SQL 里写成 `s8wiykdryzpjrrp4` 找不到

**解决方案**：

```bash
# 1. 查看实际 role 名
sudo -u postgres psql -c "\du"

# 2. SQL 里用双引号包裹 role 名（单引号包整条 SQL）
sudo -u postgres psql -d 你的数据库名 -c 'GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO "S8WiYKdRyzPJrrp4";'
```

**预防建议**：创建数据库和用户时**全用小写**，避免大小写问题。

---

### 问题 5：端口 3002 被占用

**现象**：
```
❌ 端口 3002 已被占用!
请运行以下命令清理端口:
```

**解决方案**：

```bash
# 杀掉占用 3002 的进程
fuser -k 3002/tcp

# 或者
pkill -f "node.*server.js"

# 然后重启
pm2 restart virtual-world
```

---

### 问题 6：.env 修改后不生效

**现象**：改了 `.env` 里的数据库配置，但 API 还是连旧数据库。

**原因**：Node.js 只在启动时读取一次 `.env`，运行中修改不会自动生效。

**解决方案**：

```bash
pm2 restart virtual-world
```

---

### 问题 7：sudo -u postgres 要求输入密码

**现象**：
```
Password for user postgres:
```

**原因**：宝塔面板装的 PostgreSQL 强制 MD5 认证，不允许本地 trust。

**解决方案**：

```bash
# 方式 1：用 TCP + 密码连接
PGPASSWORD='postgres的密码' psql -h 127.0.0.1 -U postgres -d 你的数据库名 -c "你的SQL"

# 方式 2：在宝塔面板里查看 postgres 密码
# 宝塔面板 → 数据库 → PostgreSQL → 管理
```

---

## 五、一键部署脚本

将以下脚本保存为 `deploy_db.sh`，部署时直接运行：

```bash
#!/bin/bash
# 数据库导入部署脚本
# 用法：bash deploy_db.sh 数据库名 用户名 密码

DB_NAME=$1
DB_USER=$2
DB_PASS=$3
PROJECT_DIR="/www/wwwroot/virtual-world-bt"

if [ -z "$DB_NAME" ] || [ -z "$DB_USER" ] || [ -z "$DB_PASS" ]; then
    echo "用法：bash deploy_db.sh 数据库名 用户名 密码"
    echo "示例：bash deploy_db.sh virtual_world vw_app MyPassword123"
    exit 1
fi

echo "=== 1. 导入数据库 ==="
sudo -u postgres psql -d "$DB_NAME" -f "$PROJECT_DIR/db_export.sql"

echo "=== 2. 权限检查 ==="
PGPASSWORD="$DB_PASS" psql -h 127.0.0.1 -U "$DB_USER" -d "$DB_NAME" -c "SELECT count(*) FROM system_config;" 2>&1

echo "=== 3. 授予权限 ==="
sudo -u postgres psql -d "$DB_NAME" -c "GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO \"$DB_USER\";"
sudo -u postgres psql -d "$DB_NAME" -c "GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO \"$DB_USER\";"
sudo -u postgres psql -d "$DB_NAME" -c "GRANT USAGE ON SCHEMA public TO \"$DB_USER\";"
sudo -u postgres psql -d "$DB_NAME" -c "ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON TABLES TO \"$DB_USER\";"
sudo -u postgres psql -d "$DB_NAME" -c "ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON SEQUENCES TO \"$DB_USER\";"

echo "=== 4. 验证权限 ==="
PGPASSWORD="$DB_PASS" psql -h 127.0.0.1 -U "$DB_USER" -d "$DB_NAME" -c "SELECT count(*) FROM system_config;"
PGPASSWORD="$DB_PASS" psql -h 127.0.0.1 -U "$DB_USER" -d "$DB_NAME" -c "SELECT nextval('ui_controls_id_seq');"

echo "=== 5. 更新 .env ==="
sed -i "s|^DB_NAME=.*|DB_NAME=$DB_NAME|" "$PROJECT_DIR/.env"
sed -i "s|^DB_USER=.*|DB_USER=$DB_USER|" "$PROJECT_DIR/.env"
sed -i "s|^DB_PASSWORD=.*|DB_PASSWORD=$DB_PASS|" "$PROJECT_DIR/.env"

echo "=== 6. 重启 Node ==="
pm2 restart virtual-world 2>/dev/null || (cd "$PROJECT_DIR" && pm2 start src/server.js --name virtual-world)
sleep 5

echo "=== 7. 测试 API ==="
curl -s http://localhost:3002/api/health
echo ""
curl -s http://localhost:3002/api/federation/info | head -c 200
echo ""

echo "=== 部署完成 ==="
```

---

## 六、最佳实践建议

### 6.1 命名规范（避免大小写问题）

| 项 | 推荐 | 不推荐 | 原因 |
|----|------|--------|------|
| 数据库名 | `virtual_world` | `VirtualWorld` | 全小写，SQL 里不用加双引号 |
| 用户名 | `vw_app` | `VwApp` | 全小写，避免引用麻烦 |
| 密码 | 可以大小写混合 | - | 密码不受影响 |

**PostgreSQL 大小写规则**：
- 未加双引号的标识符会被自动转成小写
- 混合大小写的 role/表名必须用双引号包裹
- 全小写命名可以避免所有大小写引用问题

### 6.2 权限管理方案

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| 用 postgres 超级用户 | 永无权限问题 | 安全性差 | 个人项目、开发环境 |
| 应用账号 + 完整授权 | 安全性好 | 需要授权步骤 | 生产环境（推荐） |
| 应用账号 + 改 owner | 一劳永逸 | 操作复杂 | 长期维护项目 |

### 6.3 部署前检查清单

- [ ] PostgreSQL 服务已启动（`systemctl status postgresql@版本-main`）
- [ ] 数据库已创建
- [ ] 应用账号已创建
- [ ] SQL 文件已上传到服务器
- [ ] 数据已导入（`SELECT count(*) FROM information_schema.tables`）
- [ ] 权限检查通过（表查询 + 序列查询）
- [ ] 表权限已授予（如有需要）
- [ ] 序列权限已授予（如有需要）
- [ ] .env 配置已更新
- [ ] Node 服务已重启（`pm2 restart`）
- [ ] API 测试通过（`curl /api/health`）
- [ ] PM2 开机自启已设置（`pm2 save && pm2 startup`）

---

## 七、故障排查流程图

```
API 返回 500/503
    ↓
检查 Node 服务是否在跑 → pm2 status
    ↓ ↓
    不在跑 → pm2 start src/server.js
    在跑 ↓
    ↓
检查错误日志 → pm2 logs --err
    ↓ ↓
    数据库连接错误 ↓
    ↓
测试数据库连接 → psql -h 127.0.0.1 -U 用户名 -d 数据库名
    ↓ ↓
    连不上 ↓
    ↓
检查 PostgreSQL 服务 → systemctl status postgresql@版本-main
    ↓ ↓
    没启动 → systemctl restart postgresql@版本-main
    能连上 ↓
    ↓
检查权限 → 看错误日志是否有 "permission denied"
    ↓ ↓
    有权限错误 ↓
    ↓
执行 GRANT 授权命令（见 3.5 节）
    ↓
重启 Node → pm2 restart virtual-world
    ↓
测试 API → curl /api/health
```
