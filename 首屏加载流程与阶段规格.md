# 首屏加载流程与阶段规格

> 版本 v1 / 2026-09-24。本文档是「首屏加载丝滑化 C 档」交付后的**流程规格与验收数据记录**。
> 开发过程、红线与决策记录见《首屏加载丝滑化-开发规范与进度.md》（唯一权威开发文档）。

## 一、改造后的首屏加载秩序（实现态）

| 阶段 | 系统在做什么 | 用户看到什么 | 实现模块 |
|---|---|---|---|
| 0 进入中 | 加载屏仅覆盖 API 元数据 + WS | 全屏转圈 | 未改（因他人模型不再抢带宽而更快隐藏） |
| 1 骨架瞬间 | 渲染循环启动；全图占位盒 1 draw call | 青蓝半透明盒 + 自己是几何方块人，**已可移动** | 未改 |
| 2 环境骨架 | 几何体（本地零网络）批量上屏；reveal 预热 | 盒子渐缩变真身 | 未改 |
| 3 小→中→大 | 场景 GLB 分层串行；**天空 EXR/PMREM 已让路不再抢带宽** | 远处大件逐个变真 + **程序化渐变天空先上屏** | skyManager.js v2 |
| 4 环境就绪 | `WorldReadyGate` 判定：几何体全上屏 && 队列无 <15MB 对象（连续 2 次确认），12s 硬兜底 | 无感（内部信号） | worldReadyGate.js v1 |
| 5 自己角色 | 就绪后发起模型 + 8 动画（模型就绪动画立即加载，**无 500ms×20 空转**） | 方块人 → 真模型 + 待机动画 | playerModelScheduler.js v1 |
| 6 其他玩家 | 就绪 + 自己之后；视野内（≤60m 且（视锥内或 ≤25m））最近优先，并发 1；**>15MB 场景对象让路** | 别人先是方块人，逐个替换 | playerModelScheduler.js v1 |
| 7 天空高清 | 就绪后拉 EXR 替换渐变占位 + PMREM 环境光 | 天空氛围先有、后变高清 | skyManager.js v2 |
| 8 稳态 | LOD 分带（30/60/400 后台可调）与合批常驻生效 | 走近清晰、走远变简/变盒 | 未改（LOD 全套零改动） |

## 二、同模板多玩家复用（gltfTemplateCache.js v1）

- **包装 `THREE.GLTFLoader.prototype.load`，只拦截被 register 过的 URL**（由调度器在放行玩家模型前注册）——场景模型、武器、`_mid/_lod` 变体等未注册 URL 完全走原路径零影响。
- miss = 真实加载一次 → 源 `gltf` 入缓存 → 向调用方交付**克隆**（源保持纯净，防第一人的 fitModel/缩放污染）；在途 = waiters 合并（不二次下载/解析）。
- 克隆规则：`SkeletonUtils.clone`（蒙皮骨骼正确换绑，vendor 于 `public/js/lib/three-examples/utils/`，改为从 `window.THREE` 解构）；`AnimationClip.clone`（times/values 深拷贝——animConventionCompensator 就地改 track.values，共享会串扰）；材质 `clone()` 共享纹理（材质隔离防受击闪红跨玩家串扰，显存仍 N→1）。
- 去重键 = `hostname + pathname`（忽略 scheme 与 query，覆盖 http/https 前缀分裂）。
- LRU 12：淘汰只删 Map 条目，**绝不 dispose 共享 geometry/texture**（克隆仍引用）。
- 范围边界：动画 GLB 文件**不入缓存**（§7.2 共享范围仅模型源 scene/AnimationClip[]）；跨世界不同源世界（域名不同）不合并；https 页面 ← http 源世界 = Mixed Content 物理死角（不适用）。

## 三、关键验收数字（CDP 限速 6Mbps / RTT 200ms，本机基准）

| 指标 | 改造前基线 | 改造后实测 | 达成 |
|---|---|---|---|
| 天空 EXR 请求发起 | +702ms（抢跑） | **T_ready 之后**（16213ms ≥ 13205ms），就绪前 0 条 EXR 相关请求 | ✅ |
| 场景 GLB #1 完成 | 28.2s | **10.3s（-63%）** | ✅ |
| 自己角色请求发起 | 5.2s（抢跑） | **≥ T_ready（12148ms）**，T_ready 前 0 条角色模板请求 | ✅ |
| 自己角色上屏 | 21s | T_ready + ~2.4s（0.76MB 模板，≤8s 目标内） | ✅ |
| 8 动画可用 | 19.4~22.1s（500ms×20 空转风险） | 模型就绪后即时全部绑定 | ✅ |
| 同模板 2 人解析次数 | 2 | **1**（per-key parses===1，第二人 hit 零网络请求） | ✅ |
| 远端玩家视野外加载 | 进世界即发（加载屏期间） | 200m 外 0 请求；移入 20m 才加载 | ✅ |
| 远端并发 | 无上限 | **1**（两远端请求区间不重叠） | ✅ |
| 离队取消 | 无 | 排队中出队、0 请求、0 上屏 | ✅ |
| >15MB 让路 | 无 | 玩家模型在途时假 20MB 队首 1.5s 不被消费（blocks=3） | ✅ |
| 占位渐变天空 | 黑屏等待 | avgLuma=24.7 非黑，最终被真 HDR+PMREM 完整替换 | ✅ |

## 四、验收与回归脚本（可重跑）

| 脚本 | 覆盖 | 结果 |
|---|---|---|
| `scripts/accept_first_load_ready_gate.js` | Step1 闸门 A/B 组 + Step2 天空 C 组 | 15/15 PASS |
| `scripts/accept_first_load_player_sched.js` | Step3 4 用例 + 反向断言 | 20/20 PASS |
| `scripts/smoke_r185_world.js` | 主世界冒烟 | 9/9 PASS |
| `scripts/accept_lod_stage6_texture_share.js` | LOD 合批/分带回归 | 22/22 ACCEPTED |
| `scripts/accept_lod_stage7_standalone.js` | 散装 LOD 回归 | 11/11 ACCEPTED |
| `scripts/accept_lod_stage7b_lifecycle.js` | LOD 生命周期回归 | 15/15 ACCEPTED |
| `scripts/accept_placeholder_field.js` | 占位符回归 | A3c 1 FAIL——**经决定性对照证明与本任务无关**：route 屏蔽三个新模块后同样 160/373 FAIL；环境因素（swiftshader 软渲染 + reveal 逐帧摊薄设计 + 真实在线玩家 28MB 模型主线程解析阻塞）。真 GPU 无限速下 373/373 全加载、blocks=0。该断言阈值（0.85@60s）为卡顿治理前时代口径，如需修正另行立项 |

## 五、命中矩阵（跨世界，§7.4）

| 场景 | 字节层 | 解析/显存层 |
|---|---|---|
| 同世界同模板（同一 URL） | ✅ 浏览器缓存免费 | ✅ 1 次解析 / 1 份显存 |
| 跨世界·同一源世界同模板 | ✅ 免费 | ✅ 1 次 / 1 份 |
| 跨世界·同源世界 http/https 前缀不同 | ⚠️ 可能两份 | ✅ 1 次 / 1 份（host+pathname 归一化） |
| 跨世界·不同源世界（域名不同） | ❌ 各下各的 | ❌ 各解析各的（物理上两份文件，不合并） |
| https 页面 ← 源世界只有 http | ❌ Mixed Content 被拦（停在方块人） | 不适用 |

## 六、已知坑（本任务实测沉淀）

1. `SkeletonUtils.js` 上游 import `from 'three'` 需 importmap；本地 three-shim 未导出 `AnimationMixer`/`SkeletonHelper` → vendor 版改从 `window.THREE` 解构（three.min.js 同步先加载，动态 import 时必就绪）。
2. `AnimationClip.clone()` 的 KeyframeTrack.clone 会 `times.slice(0)/values.slice(0)` 深拷贝——满足"每人动画必须克隆"要求，无需手写。
3. 验收断言 `parseCount===1` 必须按 **per-key** 判定：真实在线玩家（其他模板）会污染全局计数（本次米多在线实锤）。
4. `page.evaluate` 只接受 1 个附加参数（playwright 1.62）——老坑重犯一次，多参数必须包对象。
5. Playwright `response` 事件在**响应头到达**时触发（非 body 完成）——"请求区间不重叠"判据用 start/响应头时刻，完整下载窗口需另行测量。
6. 本世界无 >15MB 场景对象——让路包装天然路径无触发机会，验收需定向构造假队首对象。
7. 视野过滤的测试摆位必须 ≤25m（豁免区）或沿相机前向：40m 处可能恰在相机视锥外（第三人称相机朝向决定），造成"移入后不加载"的假失败。
8. 真实在线玩家会占用远端并发 1 槽位（如米多 28MB Kipfel），测试窗口需放宽且断言用 cid 定向。
9. 闸门 tick 走 interval：限速加载期主线程被解析长帧阻塞时 tick 推迟可达 ~2.5s（timeoutMs=12000 实测触发 14507ms）——验收窗口按 16s 上限。

## 七、部署同步清单（ubuntu-deploy-package）

| 文件 | 说明 |
|---|---|
| `public/js/worldReadyGate.js` | 新增 |
| `public/js/gltfTemplateCache.js` | 新增 |
| `public/js/playerModelScheduler.js` | 新增 |
| `public/js/lib/three-examples/utils/SkeletonUtils.js` | 新增（vendor） |
| `public/js/skyManager.js` | v1→v2（让路） |
| `public/index.html` | 3 个新 script 引入 + skyManager ?v=2 |
| `scripts/accept_first_load_ready_gate.js` | 新增（验收） |
| `scripts/accept_first_load_player_sched.js` | 新增（验收） |

纯前端改动，**无需重启 Node**；用户需 Ctrl+F5 强刷（index.html 改动本身 no-cache，但 skyManager 等带版本号）。
